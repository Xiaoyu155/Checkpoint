# Pacer 开发进度

更新时间：2026-07-09

| 任务 | 状态 | Commit / 说明 |
| --- | --- | --- |
| P0.1 提交现有未提交修复 | 完成 | `01fe3e4`, `0f91f8c`, `9e3668a`, `69f328d` |
| P0.2 清理仓库根目录污染 | 完成 | `40604d7` |
| P0.3 建立本计划的追踪文件 | 完成 | `75870ed` |
| P1.1 worker 未完成时禁止判 verified | 完成 | `2f6ab7d`；验收 `tests/test_chief_dispatch.py tests/test_chief_run.py -q`：95 passed |
| P1.2 失败分类器分层重写 | 完成 | `598fe9b`；验收 `tests/test_command_verification.py tests/test_chief_dispatch.py tests/test_chief_run.py -q`：112 passed |
| P1.3 验收链防篡改扩展到命令定义文件 | 完成 | `6b90072`；验收 `tests/test_acceptance_chain_guard.py tests/test_test_tamper_guard.py tests/test_chief_dispatch.py -q`：81 passed |
| P1.4 merge 门禁收口 | 完成 | `327585a`；窄验收 `tests/test_chief_dispatch.py -q`：72 passed；按用户要求未跑 Phase 1 全量 |
| P2.1 终态保护 | 完成 | `f7e3a45`；验收 `tests/test_mission_progress.py -q`：12 passed |
| P2.2 activity 改为执行侧主动上报 | 完成 | `6f22f6a`；验收 `tests/test_mission_progress.py -q`：15 passed；`tests/test_chief_dispatch.py -q`：72 passed |
| P2.3 后台 watchdog | 完成 | `9e32cdc`；验收 `tests/test_chief_background.py -q`：8 passed |
| P2.4 验证命令超时动态化 | 完成 | `00529d4`；验收 `tests/test_verification_profiles.py -q`：10 passed；`tests/test_command_verification.py tests/test_chief_dispatch.py -q`：88 passed；按用户要求未跑 Phase 2 全量与案例 A |
| P3.1 dependency preflight | 完成 | `4f05ddc`；验收 `tests/test_dependency_preflight.py -q`：3 passed；`tests/test_preflight.py -q`：5 passed；P3.2 dispatch 门禁测试留待下一任务 |
| P3.2 派发前门禁 | 完成 | `3d386e2`；验收 `tests/test_dependency_preflight.py tests/test_chief_dispatch.py tests/test_chief_run.py -q`：104 passed |
| P3.3 preflight 结果进报告 | 完成 | `f1725af`；验收 `tests/test_dependency_preflight.py tests/test_chief_dispatch.py tests/test_chief_run.py -q`：105 passed |
| P4.1 final report 三段式改造 | 完成 | `8871eb6`；验收 `tests/test_chief_run.py tests/test_chief_dispatch.py -q`：102 passed；按用户要求未跑全量与 codex-check |
| P4.2 plan/report 的 verification_mode 收口 | 完成 | `ff63ead`；验收 `tests/test_chief_run.py tests/test_codex_check.py tests/test_dashboard.py -q`：70 passed；按用户要求未跑全量、codex-check 与 dogfood |
| P4.3 dashboard 显示 activity 与耗时 | 完成 | `e03742c`；验收 `tests/test_chief_run.py::test_final_report_has_three_sections tests/test_dashboard.py -q`：28 passed；按用户要求未跑全量、codex-check 与 dogfood |
| P5 跨项目 dogfood 验证 | 完成 | `8bf3ad6`；案例 A/B/C 各连续 3 轮零误报、零误 merge、零状态误导；详见 Phase 5 记录 |

## Phase 0 记录

- P0.1 按既有狗粮修复拆成四组提交：信任内核、dashboard 状态/配置、CLI/质量/队列辅助、计划文档与工作流样例。
- P0.2 已把 `xiao/` 根目录运行产物归档到 `xiao/.agent-workspace/p0-root-cleanup-20260709/`；聚合强制测试记录归档到 `xiao/.agent-workspace/missions/p0-root-cleanup-20260709/强制测试记录.md`。
- P0.2 同步清理了 Git 顶层 `D:\助手codex` 的 `runs/` 与 `强制测试记录.md`，归档到顶层 `.agent-workspace`。
- `xiao/model_api_keys.txt` 保持 ignored，未移动也未读取内容。

## Phase 5 记录

- 2026-07-09 P5 案例 B 第 1 轮（mission `p5-case-b-preflight-20260709`，plan `20260709-084207-ae51fc`）：在未设置 `QWEN_API_KEY` 时执行 `chief-run --execute --test-command "npm run eval:acceptance" --require-env QWEN_API_KEY`，结果为 `preflight_blocked` / `verification_environment_missing`；worktree `created=false`，`worker_records=0`，外部仓库 `D:\抖音快手支付宝` 保持干净。发现并修复遗漏：阻断/验收失败等非运行态不再复用 stale activity，避免 dashboard 显示误导性的 `Worker output` / `Running verification`。验收：`python -m pytest tests/test_mission_progress.py`：17 passed；`python -m pytest tests/test_dashboard.py::test_dashboard_data_and_detail_propagate_activity_fields`：1 passed。Commit：`bd59012`。
- 2026-07-09 补跑 P4 遗漏的 `codex-check`（未使用 `pytest -q`）：`python -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root . --format markdown`，结果 PASS；`checkout_verification` 与 `pacer_workbench_static_acceptance` 均 OK，真实交互 2 passed，failed 0。
- 2026-07-09 P5 案例 C 第 1 轮（mission `p5-case-c-self-20260709`，plan `20260709-085232-63995e`）：未使用 `-q`，命令为 `python -m pytest tests/test_mission_progress.py tests/test_chief_run.py`；结果未误报 verified、未 merge，停止为 `budget_exhausted`，最新验收 `verification_failed`。发现真实问题：当 `--repo-root` 是 Git 顶层下的已跟踪子目录（本仓库的 `xiao/`）时，Pacer 在 worktree 顶层执行 worker/验收，导致相对测试路径错位；同时 dirty gate 会把 `xiao/.agent-workspace/` 误判为用户脏改。已修复为 worker cwd、命令验收 repo_root、worktree workspace 都映射到 worktree 内对应子目录，并同时忽略 Git 顶层相对的 workspace 路径。验收：`python -m pytest tests/test_chief_dispatch.py::test_chief_dispatch_nested_tracked_repo_root_runs_inside_worktree_subdir`：1 passed；`python -m pytest tests/test_chief_dispatch.py::test_create_worktree_overlays_nested_untracked_project_when_dirty_allowed tests/test_chief_dispatch.py::test_workspace_record_dirty_prefixes_ignore_whole_workspace`：2 passed；`python -m pytest tests/test_chief_dispatch.py::test_chief_dispatch_execute_records_worker_and_verification`：1 passed。Commit：`7bc549f`。
- 2026-07-09 P5 案例 C 第 2 轮（mission `p5-case-c-self-rerun-20260709`，plan `20260709-090628-cd0f90`）：未使用 `-q`，命令为 `python -m pytest tests/test_mission_progress.py`；结果 `verified`，未 merge。worker cwd 已正确映射到 `...\track-1-codex\xiao`；验收 17 passed；`test_files_changed=[]`、`acceptance_chain_files_changed=[]`，无假 verified、无误 merge、无状态误导。发现并修复遗漏：目标文本中的“保留所有验收配置不变 / 不要修改测试”不应触发 `allow_test_edits`，否则会弱化验收链防篡改；已加入否定短语剔除并收紧“验收/评测/回归/eval”推断模式。验收：`python -m pytest tests/test_chief_run.py::test_objective_test_edit_inference_handles_qualified_phrases`：1 passed；`python -m pytest tests/test_chief_run.py::test_chief_run_allows_test_edits_when_objective_requests_tests tests/test_chief_run.py::test_chief_run_with_test_command_bootstraps_missing_workspace`：2 passed。Commit：`13175b5`。
- 2026-07-09 P5 案例 A dry-run/preflight（missions `p5-case-a-preflight-20260709` / `p5-case-a-preflight-rerun-20260709`）：未使用 `-q`，未启动 worker，worktree `created=false`，未运行 `npm ci`；外部仓库 `D:\宠物小程序\backend` 运行前已有未提交/未跟踪文件，因此只用 `--dry-run --allow-dirty` 做预检。首次 dry-run 发现 dependency preflight 已提示 `node_dependencies_not_installed` / estimated 9 minutes，但条件命令 `if not exist node_modules\express\package.json npm ci ...` 的 timeout 仍为 60 秒。已修复条件 npm install marker 缺失时的 timeout 估算，复跑后 `verification_timeout.timeout_seconds=1260.0`，reason `missing_node_dependency_marker`。验收：`python -m pytest tests/test_verification_profiles.py::test_timeout_extended_when_conditional_npm_ci_marker_missing tests/test_verification_profiles.py::test_timeout_unchanged_when_conditional_npm_ci_marker_present tests/test_verification_profiles.py::test_timeout_unchanged_when_deps_present`：3 passed。Commit：`3be2bed`。
- 2026-07-09 P5 变更后补跑 `codex-check`（未使用 `pytest -q`）：`python -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root . --format markdown`，结果 PASS；`checkout_verification` 与 `pacer_workbench_static_acceptance` 均 OK，真实交互 2 passed，failed 0。
- 2026-07-09 P5 案例 B 第 2 轮（mission `p5-case-b-preflight-r2-20260709`，plan `20260709-094357-9d9a8b`）：未使用 `-q`；在当前进程显式清空 `QWEN_API_KEY` 后执行 `chief-run --execute --test-command "npm run eval:acceptance" --require-env QWEN_API_KEY`，结果为 `preflight_blocked` / `verification_environment_missing`，CLI 因预检阻断返回 1；`worker_records=0`，`workers.jsonl` 未生成，worktree 路径未实际创建，外部仓库 `D:\抖音快手支付宝` 保持干净；`progress.activity=""`，无假 verified、无误 merge、无状态误导。Commit：`d7bb44f`。
- 2026-07-09 P5 案例 C 第 3 轮（mission `p5-case-c-self-r3-20260709`，plan `20260709-094546-2ba97f`）：未使用 `-q`，命令为 `python -m pytest tests/test_mission_progress.py`；结果 `verified`，未 merge。worker `status=completed`、cwd 为 `...\track-1-codex\xiao`；主控验收 17 passed，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`；worktree 仅修改 `xiao/docs/开发进度.md` 1 行，`progress.activity=""`，无假 verified、无误 merge、无状态误导。主仓补跑 `python -m pytest tests/test_mission_progress.py`：17 passed。Commit：`18f4834`。
- 2026-07-09 P5 案例 B 第 3 轮（mission `p5-case-b-preflight-r3-20260709`，plan `20260709-095040-418227`）：未使用 `-q`；在当前进程显式清空 `QWEN_API_KEY` 后执行 `chief-run --execute --test-command "npm run eval:acceptance" --require-env QWEN_API_KEY`，结果为 `preflight_blocked` / `verification_environment_missing`，CLI 因预检阻断返回 1；`worker_records=0`，`workers.jsonl` 未生成，worktree 路径未实际创建，外部仓库 `D:\抖音快手支付宝` 保持干净；`progress.activity=""`，无假 verified、无误 merge、无状态误导。Commit：`1be43e8`。
- 2026-07-09 P5 案例 A dry-run/preflight 第 3 轮（mission `p5-case-a-preflight-r3-20260709`，plan `20260709-095141-6c0fbc`）：未使用 `-q`，只执行 `chief-run --dry-run --allow-dirty`，未启动 worker、未创建 worktree、未运行 `npm ci`；外部仓库 `D:\宠物小程序\backend` 保持原有脏改状态。预检结果 `preview_only`，dependency 仍提示 `node_dependencies_not_installed`，条件命令 `if not exist node_modules\express\package.json npm ci ...` 的 `verification_timeout.timeout_seconds=3000.0`，reason `missing_node_dependency_marker`；`progress.activity=""`，无假 verified、无误 merge、无状态误导。Commit：`b2ab97d`。
- 2026-07-09 P5 案例 C 第 4 轮（mission `p5-case-c-self-r4-20260709`，plan `20260709-095254-3a2887`）：未使用 `-q`，命令为 `python -m pytest tests/test_mission_progress.py`；结果 `verified`，未 merge。worker `status=completed`、cwd 为 `...\track-1-codex\xiao`；主控验收 17 passed，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`；worktree 仅修改 `xiao/docs/开发进度.md` 1 行，`progress.activity=""`，无假 verified、无误 merge、无状态误导。worker 自身 Python 环境缺 pytest/pip，但主控侧验收在 worktree 正确通过；主仓补跑 `python -m pytest tests/test_mission_progress.py`：17 passed。Commit：`10f87d4`。
- 2026-07-09 P5 案例 A 第 4 轮预备（mission `p5-case-a-execute-r4-20260709`，plan `20260709-105505-4ceb38`）：未使用 `-q`；先 dry-run 固定隔离 worktree，再用 Pacer `create_worktree(..., allow_dirty=True)` 复制原仓脏上下文为 baseline，随后仅在 worktree 内执行 `npm ci --cache .npm-cache --prefer-offline`（8 分钟，262 packages），原仓 `D:\宠物小程序\backend` 未新增变化。发现真实遗漏：计划命令 `if not exist node_modules\express\package.json npm ci ... && node --test ...` 在 marker 已存在时会空跑并返回 0，导致 `node --test` 未执行，存在假 verified 风险。已新增 `conditional_test_command_short_circuit` 保护，dispatch preflight 与 command gate 均会拒绝该短路命令。验收：`python -m pytest tests/test_verification_profiles.py::test_conditional_npm_ci_command_blocks_when_marker_present tests/test_verification_profiles.py::test_conditional_npm_ci_command_allows_missing_marker tests/test_verification_profiles.py::test_conditional_npm_ci_command_allows_non_short_circuit_form`：3 passed；`python -m pytest tests/test_command_verification.py::test_conditional_npm_ci_short_circuit_blocks_before_run`：1 passed；`python -m pytest tests/test_chief_dispatch.py::test_chief_dispatch_blocks_conditional_npm_ci_short_circuit`：1 passed；相邻回归 8 passed。Commit：`a9f1570`。
- 2026-07-09 P5 案例 A 第 4 轮执行（mission `p5-case-a-execute-r4-20260709`，plan `20260709-105505-4ceb38`）：未使用 `-q`，用 Node 包装命令确保依赖存在时仍执行 `node --test`；结果为 `no_product_changes`，未 verified、未 merge。worker `status=completed`、验收命令真实输出 TAP，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`、7 tests passed；`changed_product_file_count=0`，`progress.activity=""`。该轮证明无产品改动时不会因测试通过而误报 verified。
- 2026-07-09 P5 案例 A 第 5 轮（mission `p5-case-a-execute-r5-20260709`，plan `20260709-135106-07431e`）：未使用 `-q`；隔离 worktree 任务在 `pacer-one-hour-plan-short.md` 末尾追加 1 行 dogfood 记录，未修改测试、`package.json`、`package-lock.json` 或验收配置，未 merge。首次验收暴露 Windows `spawnSync('npm')` 找不到 npm 的包装问题，未误报 verified；改用 Windows 下 `npm.cmd` + `shell:true` 后 resume 成功，结果 `verified`，`worker_records=2`，主控验收 TAP 7 tests passed，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`，`progress.activity=""`，外部原仓 `D:\宠物小程序\backend` 保持原有脏改状态，无假 verified、无误 merge、无状态误导。Commit：`6b717d6`。
- 2026-07-09 P5 dispatch/verify 变更后补跑 `codex-check`（未使用 `pytest -q`）：`python -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root . --format markdown`，结果 PASS；`checkout_verification` 与 `pacer_workbench_static_acceptance` 均 OK，真实交互 2 passed，inspection-only 0，failed 0。Commit：`a1b5b79`。
- 2026-07-09 P5 案例 A 第 6 轮（mission `p5-case-a-execute-r6-20260709`，plan `20260709-140754-6d9d6d`）：未使用 `-q`；隔离 worktree 任务在 `pacer-dogfood-portion-plan.md` 末尾追加 1 行 dogfood 记录，未修改测试、`package.json`、`package-lock.json` 或验收配置，未 merge。结果 `verified`，`worker_records=1`，主控验收 TAP 7 tests passed，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`，`progress.activity=""`，worktree 仅修改目标文件 1 行，外部原仓保持原有脏改状态，无假 verified、无误 merge、无状态误导。
- 2026-07-09 P5 案例 A 第 7 轮（mission `p5-case-a-execute-r7-20260709`，plan `20260709-141437-c0594d`）：未使用 `-q`；隔离 worktree 任务在 `pacer-supervised-one-hour-plan.md` 末尾追加 1 行 dogfood 记录，未修改测试、`package.json`、`package-lock.json` 或验收配置，未 merge。结果 `verified`，`worker_records=1`，主控验收 TAP 7 tests passed，`test_files_changed=[]`、`acceptance_chain_files_changed=[]`，`progress.activity=""`，worktree 仅修改目标文件 1 行，外部原仓保持原有脏改状态，无假 verified、无误 merge、无状态误导。
- 2026-07-09 P5 毕业判定：案例 A 第 5-7 轮为连续 verified 闭环；案例 B 第 1-3 轮连续 `preflight_blocked` 且不建 worktree/不启动 worker；案例 C 第 2-4 轮连续 verified 闭环。三案均满足连续 3 轮零假 verified、零误 merge、零状态误导。Commit：`8bf3ad6`。
