# 验收进度（2026-07-10）

## 验收纪律

- 不执行任何带 `-q` 的命令。
- 每一步只运行一个窄范围命令或一个测试文件。
- 每一步结束后记录命令、结果摘要和下一步。
- 失败或中断时先记录，再决定是否继续扩大范围。

## 初始状态

- 时间：2026-07-10 Asia/Shanghai
- 仓库：`D:\助手codex`
- HEAD：`cbe267b 补强执行层并整理云端部署`
- 工作区：干净（`git status --short` 无输出）
- Python：`Python 3.13.3`

## Step 1：环境和仓库状态确认

- 命令：`git status --short`
- 结果：通过。无输出，说明工作区干净。
- 命令：`git log -1 --oneline`
- 结果：通过。当前提交为 `cbe267b 补强执行层并整理云端部署`。
- 命令：解释器探测，不包含 `-q`
- 结果：通过。当前可用 Python 为 `Python 3.13.3`。
- 下一步：运行只读文件状态检查和基础 Git 检查。

## Step 2：只读 Git/文件卫生检查

- 命令：`git diff --check`
- 结果：通过。无空白错误输出。
- 命令：`rg -n "^(<<<<<<<|=======|>>>>>>>)" .gitignore .github render.yaml xiao`
- 结果：通过。`rg` 返回 1，表示未命中冲突标记。
- 命令：`git status --short`
- 结果：通过。当前仅有本验收进度文件未跟踪。
- 命令：`git ls-files --others --exclude-standard`
- 结果：通过。未跟踪文件仅为 `xiao/docs/验收进度_2026-07-10.md`。
- 下一步：探测 pytest 可用性，不使用 `-q`。

## Step 3：测试工具和测试清单探测

- 命令：`..\.venv\Scripts\python.exe -m pytest --version`
- 结果：通过。`pytest 9.0.3`。
- 命令：`..\.venv\Scripts\python.exe -m pip --version`
- 结果：通过。`pip 25.0.1`，运行在仓库 `.venv`。
- 命令：`rg --files tests`
- 结果：通过。测试文件清单可枚举，包含单元测试、e2e 测试和 benchmark fixtures。
- 下一步：按单文件粒度运行核心测试，不使用 `-q`。

## Step 4：提交影响面确认

- 命令：`git show --name-only --format=oneline --no-renames HEAD`
- 结果：通过。当前提交覆盖执行层、Chief 调度、Cloud/API、Dashboard、CI/Render 配置、打包配置、benchmark fixtures 和对应测试。
- 验收范围：优先运行当前提交直接相关的单元测试文件，每次只运行一个文件，不使用 `-q`。
- 下一步：从底层动作和执行包装测试开始。

## Step 5：执行动作层测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests/test_actions.py`
- 结果：通过。收集 8 个用例，8 个通过，用时 1.51s。
- 下一步：运行 `tests/test_codex_exec.py`。

## Step 6：Codex 执行封装测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests/test_codex_exec.py`
- 结果：通过。收集 12 个用例，12 个通过，用时 0.13s。
- 下一步：运行 `tests/test_subprocess_window.py`。

## Step 7：子进程窗口封装测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests/test_subprocess_window.py`
- 结果：通过。收集 5 个用例，5 个通过，用时 0.06s。
- 下一步：运行 `tests/test_chief_queue.py`。

## Step 8：队列测试执行前安全审查

- 原计划命令：`..\.venv\Scripts\python.exe -m pytest tests/test_chief_queue.py`
- 结果：未执行。静态检查确认该文件原有 12 个用例，其中最后 3 个锁测试会把当前 pytest PID 写入 worker lock；`_acquire_worker_lock` 在 Windows 上使用 `os.kill(pid, 0)` 探测存活进程，存在终止当前 pytest 及其外层会话的风险。
- 静态核对：读取 `src/visual_agent/chief_queue.py`、`tests/test_chief_queue.py`、`git show HEAD -- src/visual_agent/chief_queue.py tests/test_chief_queue.py` 和相关 `git blame`。风险代码来自更早提交，不是 HEAD 新增，但会被本次验收路径触发。
- 决定：禁止运行原测试文件及任何真实 PID 的 `os.kill(..., 0)` 探针；先改为 Windows 无信号的进程状态检查，并让锁测试 mock PID 存活结果。
- 下一步：完成修复后，只运行不包含 3 个锁场景的安全队列测试。

## Step 9：Windows worker lock 安全修复

- 修改：`chief_queue._pid_is_running` 委托给已有的 `chief_background.process_status`；后者在 Windows 使用 `OpenProcess/GetExitCodeProcess`，不向目标 PID 发送信号。
- 修改：3 个锁测试显式 mock `_pid_is_running`，避免测试接触真实 PID；新增一个只验证安全委托关系的回归用例。
- 下一步：先做静态语法和差异检查，再运行安全筛选后的队列测试。

## Step 10：队列修复语法检查

- 命令：`..\.venv\Scripts\python.exe -m py_compile src\visual_agent\chief_queue.py tests\test_chief_queue.py`
- 结果：通过。命令返回 0，无错误输出；该检查只编译文件，不执行 PID 探针。
- 下一步：运行 `tests/test_chief_queue.py` 的安全子集，明确排除 3 个锁场景。

## Step 11：队列测试安全子集

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_queue.py -k "not test_worker_lock_blocks_second_watch_worker and not test_stale_worker_lock_is_overwritten and not test_watch_worker_blocked_by_existing_lock"`
- 结果：通过。收集 13 个用例，明确排除 3 个锁场景，运行其余 10 个；10 个全部通过，用时 0.66s。
- 未覆盖：按安全要求，未执行 `test_worker_lock_blocks_second_watch_worker`、`test_stale_worker_lock_is_overwritten`、`test_watch_worker_blocked_by_existing_lock`。这 3 个测试已改为 mock PID 存活检查，但本轮仍不运行。
- 下一步：重新读取 HEAD 影响文件清单，选择下一个直接相关测试文件。

## Step 12：后台执行测试安全审查

- 命令：`rg -n "^def test_|os\\.kill|taskkill|terminate_process|process_status|subprocess\\.(Popen|run)|os\\._exit|sys\\.exit|current_process|os\\.getpid" tests\\test_chief_background.py`
- 结果：通过静态安全审查。文件共 9 个测试；子进程启动、进程状态探测和超时终止路径均使用 fake、`process_probe` 或 monkeypatch，没有向当前 PID 发送真实信号的测试路径。
- 下一步：运行 `tests/test_chief_background.py`。

## Step 13：后台执行测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_background.py`
- 结果：通过。收集 9 个用例，9 个通过，用时 1.54s。
- 下一步：安全审查并运行 `tests/test_chief_dispatch.py`。

## Step 14：Chief 调度测试安全审查

- 命令：`rg -n "^def test_|os\\.kill|taskkill|subprocess\\.(Popen|run)|os\\._exit|sys\\.exit|Codex|codex" tests\\test_chief_dispatch.py`
- 结果：通过静态安全审查。真实子进程限于临时 Git 仓库操作和测试自行创建的隔离子进程；超时终止作用于子进程句柄。Codex/Claude 执行路径使用 fake runner，没有针对当前 pytest PID 的终止路径。
- 下一步：运行 `tests/test_chief_dispatch.py`。

## Step 15：Chief 调度测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_dispatch.py`
- 结果：通过。收集 92 个用例，92 个通过，用时 32.39s。
- 下一步：安全审查并运行 `tests/test_chief_run.py`。

## Step 16：Chief 总编排测试安全审查

- 命令：`rg -n "^def test_|os\\.kill|taskkill|terminate_process|subprocess\\.(Popen|run)|os\\._exit|sys\\.exit|os\\.getpid|process_status" tests\\test_chief_run.py`
- 结果：通过静态安全审查。未发现进程终止、当前 PID 探测或真实子进程启动路径；测试使用注入的 dispatch runner 验证编排状态。
- 下一步：运行 `tests/test_chief_run.py`。

## Step 17：Chief 总编排测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_run.py`
- 结果：通过。收集 35 个用例，35 个通过，用时 2.70s。
- 下一步：运行 `tests/test_chief_plans_store.py`。

## Step 18：Chief 计划存储测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_plans_store.py`
- 结果：通过。收集 13 个用例，13 个通过，用时 0.44s。
- 下一步：安全审查并运行 `tests/test_cloud_api.py`。

## Step 19：云 API 测试安全审查

- 命令：`rg -n "^def test_|requests\\.|httpx\\.|urlopen|socket\\.|subprocess|Popen|os\\.kill|taskkill|TestClient|monkeypatch" tests\\test_cloud_api.py`
- 结果：通过静态安全审查。HTTP 场景使用 FastAPI `TestClient` 进程内调用，身份和执行器均被隔离；没有真实网络、服务器进程或 PID 操作。
- 下一步：运行 `tests/test_cloud_api.py`。

## Step 20：云 API 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_cloud_api.py`
- 结果：通过。收集 19 个用例，19 个通过，用时 7.65s。
- 警告：1 条 `StarletteDeprecationWarning`，来自 FastAPI `TestClient` 对当前 `httpx` 集成的弃用提示，非本次业务断言失败。
- 下一步：安全审查并运行 `tests/test_cloud_server.py`。

## Step 21：本地云服务器测试安全审查

- 命令：`rg -n "^def test_|listen|serve|socket\\.|requests\\.|httpx\\.|urlopen|subprocess|Popen|os\\.kill|taskkill|monkeypatch" tests\\test_cloud_server.py`
- 结果：通过静态安全审查。测试会绑定 `127.0.0.1` 随机端口并做回环 HTTP 请求，不访问外网；服务器均在 `finally` 中关闭，没有 PID 信号或外部进程操作。
- 下一步：运行 `tests/test_cloud_server.py`。

## Step 22：本地云服务器测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_cloud_server.py`
- 结果：通过。收集 19 个用例，19 个通过，用时 20.74s。
- 下一步：运行 `tests/test_dashboard.py`。

## Step 23：Dashboard 测试（首次）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard.py`
- 结果：失败。收集 32 个用例，31 个通过、1 个失败，用时 4.57s。
- 失败：`test_start_worker_uses_cli_module_entrypoint` 期望 `start_worker(...)` 返回 `ok=True`，实际为 `False`。
- 决定：不立即重跑；先静态核对 `start_worker/stop_worker` 实现、失败测试上下文及全局状态管理，确定根因后再决定修复范围。
- 下一步：读取 Dashboard worker 生命周期相关代码和测试。

## Step 24：Dashboard 失败隔离复现

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard.py::test_start_worker_uses_cli_module_entrypoint`
- 结果：通过。单独收集并运行 1 个用例，1 个通过，用时 0.21s。
- 根因：前面的 Dashboard 服务器测试会初始化模块级 active workspace；失败用例直接用另一个 `tmp_path` 调用 `start_worker`，未建立“当前项目”前置条件，因此整文件运行时被新增的项目切换保护正确拒绝。该测试存在顺序依赖。
- 修复：测试先创建 `.agent-workspace` 并调用 `set_active_workspace(workspace)`，再调用 `start_worker(workspace)`，与实际 API 生命周期一致。
- 下一步：重跑整个 `tests/test_dashboard.py`。

## Step 25：Dashboard 测试（第二次）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard.py`
- 结果：失败。收集 32 个用例，31 个通过、1 个失败，用时 4.56s；Step 23 的 worker 用例已通过。
- 新失败：`test_dashboard_api_requires_json_content_type` 在等待预期的 HTTP 415 响应时收到 `ConnectionAbortedError: [WinError 10053]`，连接被本机软件中止。
- 决定：先检查 Dashboard POST 内容类型拒绝分支及服务器线程收尾，再隔离运行该测试，区分稳定实现问题和回环时序抖动。
- 下一步：读取请求校验与错误响应代码。

## Step 26：Dashboard 415 失败隔离复现

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard.py::test_dashboard_api_requires_json_content_type`
- 结果：通过。单独收集并运行 1 个用例，1 个通过，用时 0.74s。
- 静态结论：415 分支通过 `_send_json` 写入完整 JSON、`Content-Length` 并返回；未发现服务端异常日志。当前失败表现为 Windows 回环连接瞬时中止，尚不能稳定复现为实现缺陷。
- 下一步：第三次运行整个 `tests/test_dashboard.py`；若同一路径再次失败，则修复测试服务器同步。

## Step 27：Dashboard 测试（第三次）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard.py`
- 结果：通过。收集 32 个用例，32 个通过，用时 4.51s。
- 结论：worker 用例顺序依赖修复有效；Step 25 的 Windows 回环 `WinError 10053` 未再出现，保留为一次性环境抖动记录。
- 下一步：运行 `tests/test_dashboard_workbench.py`。

## Step 28：Dashboard Workbench 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard_workbench.py`
- 结果：通过。收集 24 个用例，24 个通过，用时 3.65s。
- 下一步：运行 `tests/test_execution_alignment.py`。

## Step 29：执行对齐测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_execution_alignment.py`
- 结果：通过。收集 7 个用例，7 个通过，用时 0.19s。
- 下一步：运行 `tests/test_execution_benchmarks.py`。

## Step 30：执行 Benchmark 定义测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_execution_benchmarks.py`
- 结果：通过。收集 10 个用例，10 个通过，用时 4.89s。
- 下一步：安全审查并运行 `tests/test_execution_benchmark_runner.py`。

## Step 31：执行 Benchmark Runner 安全审查

- 命令：`rg -n "^def test_|subprocess|Popen|os\\.kill|taskkill|codex|claude|requests\\.|httpx\\.|urlopen|monkeypatch" tests\\test_execution_benchmark_runner.py`
- 结果：通过静态安全审查。超时终止和 Codex 调用均由 fake/injected runner 接管，不启动真实 agent，不作用于当前 PID。
- 下一步：运行 `tests/test_execution_benchmark_runner.py`。

## Step 32：执行 Benchmark Runner 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_execution_benchmark_runner.py`
- 结果：通过。收集 18 个用例，18 个通过，用时 1.05s。
- 下一步：运行 `tests/test_goal_intake.py`。

## Step 33：目标解析测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_goal_intake.py`
- 结果：通过。收集 12 个用例，12 个通过，用时 0.12s。
- 下一步：安全审查并运行 `tests/test_locks.py`。

## Step 34：文件锁测试安全审查

- 命令：`rg -n "^def test_|os\\.kill|taskkill|pid|process|subprocess|Popen|terminate" tests\\test_locks.py src\\visual_agent\\locks.py`
- 结果：通过静态安全审查。PID 仅作为文件锁元数据，不进行进程探测或终止；测试使用文件锁和线程等待。
- 下一步：运行 `tests/test_locks.py`。

## Step 35：文件锁测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_locks.py`
- 结果：通过。收集 6 个用例，6 个通过，用时 0.19s。
- 警告：1 条 `UserWarning`，来自测试刻意模拟未完整写入的锁文件；实现将其视为初始化中的活跃锁，符合断言。
- 下一步：运行 `tests/test_missions.py`。

## Step 36：Mission 数据测试（首次）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_missions.py`
- 结果：失败。收集 15 个用例，14 个通过、1 个失败，用时 0.47s。
- 失败：`test_default_budget_policy_caps_first_repair_round` 期望 `max_repair_rounds == 1`，实际默认值为 `2`。
- 决定：核对 HEAD 对预算策略、Chief repair loop 和文档的修改，确认预期值后再修复实现或测试。
- 下一步：读取 `default_budget_policy`、相关调用与提交差异。

## Step 37：Mission 默认修复预算断言更新

- 结论：HEAD 明确将默认 `max_rounds` 从 2 提升到 3，并将默认 `max_repair_rounds` 提升到 2；`tests/test_cli.py` 已断言默认值 2，`tests/test_chief_dispatch.py` 也覆盖两轮 repair ledger。实现值 2 符合本次提交设计。
- 修复：将 7 月 2 日遗留测试改名为 `test_default_budget_policy_caps_repair_rounds_at_two`，注释和断言同步为默认最多 2 个自动修复轮次。
- 下一步：重跑 `tests/test_missions.py`。

## Step 38：Mission 数据测试（复验）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_missions.py`
- 结果：通过。收集 15 个用例，15 个通过，用时 0.38s。
- 下一步：运行 `tests/test_model_credentials.py`。

## Step 39：模型凭据测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_model_credentials.py`
- 结果：通过。收集 14 个用例，14 个通过，用时 0.36s。
- 下一步：运行 `tests/test_program_scheduler.py`。

## Step 40：Program Scheduler 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_program_scheduler.py`
- 结果：通过。收集 10 个用例，10 个通过，用时 6.21s。
- 下一步：运行 `tests/test_security.py`。

## Step 41：安全测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_security.py`
- 结果：通过。收集 20 个用例，20 个通过，用时 0.23s。
- 下一步：运行 `tests/test_workbench_board.py`。

## Step 42：Workbench Board 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_workbench_board.py`
- 结果：通过。收集 20 个用例，20 个通过，用时 0.39s。
- 下一步：运行 `tests/test_ci_templates.py`。

## Step 43：CI 模板测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_ci_templates.py`
- 结果：通过。收集 6 个用例，6 个通过，用时 0.34s。
- 下一步：安全审查并运行 `tests/test_cli.py`。

## Step 44：CLI 测试安全审查

- 命令：`rg -n "^def test_|subprocess\\.(run|Popen)|os\\.kill|taskkill|terminate_process|os\\._exit|sys\\.exit|urlopen|requests\\.|httpx\\.|codex exec|pytest -q|\\-q" tests\\test_cli.py`
- 结果：通过静态安全审查。真实子进程仅用于临时 Git 仓库；`pytest -q` 仅作为 CLI 计划输出字符串断言，不会被执行；没有当前 PID 探针或真实 Codex 调用。
- 下一步：运行 `tests/test_cli.py`。

## Step 45：CLI 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_cli.py`
- 结果：通过。收集 66 个用例，66 个通过，用时 22.59s。
- 下一步：枚举其余既有测试，补齐被修改源码模块的回归覆盖。

## Step 46：既有回归测试补充清单

- 命令：`rg --files tests -g 'test_*.py'`
- 结果：完成枚举。除已运行的 HEAD 直接修改测试外，选出与本次源码变更直接关联的既有测试：agent capabilities、Chief engineer/demo、command verification、templates、workflow generator、workspace、队列消费者和 Dashboard 静态验收。
- 下一步：运行 `tests/test_agent_capabilities.py`。

## Step 47：Agent capabilities 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_agent_capabilities.py`
- 结果：通过。收集 8 个用例，8 个通过，用时 0.35s。
- 下一步：按用户补充要求更新 MiMo 专属 key。

## Step 48：MiMo 专属 key 更新

- 来源：`D:\Projects\国产模型api组.txt` 中唯一的“专属新的key”条目；未回显明文。
- 目标：项目根目录已忽略的 `model_api_keys.txt` 中唯一 `mimo` 条目。
- 结果：更新成功。来源与目标 SHA-256 前 12 位均为 `FDE250596543`，`MATCH=True`，MiMo 条目数为 1；`git check-ignore model_api_keys.txt` 确认仍被忽略。
- 下一步：重跑 `tests/test_model_credentials.py`。

## Step 49：模型凭据测试（MiMo key 更新后）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_model_credentials.py`
- 结果：通过。收集 14 个用例，14 个通过，用时 0.33s。
- 下一步：运行 `tests/test_chief_engineer.py`。

## Step 50：Chief Engineer 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_engineer.py`
- 结果：通过。收集 15 个用例，15 个通过，用时 0.73s。
- 下一步：运行 `tests/test_chief_run_demo.py`。

## Step 51：Chief Demo 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_chief_run_demo.py`
- 结果：通过。收集 2 个用例，2 个通过，用时 0.54s。
- 下一步：运行 `tests/test_command_verification.py`。

## Step 52：Command Verification 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_command_verification.py`
- 结果：通过。收集 17 个用例，17 个通过，用时 0.91s。
- 下一步：运行 `tests/test_templates.py`。

## Step 53：模板测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_templates.py`
- 结果：通过。收集 7 个用例，7 个通过，用时 5.55s。
- 下一步：运行 `tests/test_workflow_generator.py`。

## Step 54：Workflow Generator 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_workflow_generator.py`
- 结果：通过。收集 13 个用例，13 个通过，用时 0.47s。
- 下一步：运行 `tests/test_workspace.py`。

## Step 55：Workspace 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_workspace.py`
- 结果：通过。收集 32 个用例，32 个通过，用时 14.51s。
- 下一步：运行 `tests/test_portfolio_worker.py`。

## Step 56：Portfolio Worker 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_portfolio_worker.py`
- 结果：通过。收集 4 个用例，4 个通过，用时 0.31s。
- 下一步：运行 `tests/test_mission_facade.py`。

## Step 57：Mission Facade 测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_mission_facade.py`
- 结果：通过。收集 4 个用例，4 个通过，用时 1.06s。
- 下一步：运行 `tests/test_mission_plan_import.py`。

## Step 58：Mission Plan Import 测试（首次）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_mission_plan_import.py`
- 结果：失败。收集 8 个用例，7 个通过、1 个失败，用时 0.88s。
- 失败：`test_mission_import_facade_outputs_json` 使用 `--create --queue` 时，顶层 `status` 实际为 `created`，期望为 `queued`。
- 决定：核对 import facade 的队列结果聚合与输出契约，再修复实现或测试。
- 下一步：读取 mission import 实现、CLI facade 和提交差异。

## Step 59：Mission Import 验收命令测试修复

- 根因：失败用例在没有 `pyproject.toml`、`package.json` 或 tests 标记的临时 repo 中传入 `--test-command auto`。自动检测按设计返回 `not_found`，mission 因验收命令未解析而只创建不入队；顶层 `status=created` 是正确的 fail-closed 行为。
- 修复：该用例的目标是验证 facade JSON 与队列字段透传，因此改用显式字符串 `python -m pytest tests/test_security.py`；测试不会执行该命令。
- 下一步：重跑 `tests/test_mission_plan_import.py`。

## Step 60：Mission Plan Import 测试（复验）

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_mission_plan_import.py`
- 结果：通过。收集 8 个用例，8 个通过，用时 1.16s。
- 下一步：运行 `tests/test_dashboard_static_acceptance.py`。

## Step 61：Dashboard 静态验收

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard_static_acceptance.py`
- 结果：通过。收集 2 个用例，2 个通过，用时 0.10s。
- 下一步：检查 Dashboard 浏览器测试安全边界，并按 `AGENTS.md` 运行 Checkpoint fast check。

## Step 62：Dashboard 真实浏览器验收

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_dashboard_browser.py`
- 结果：通过。收集 8 个 Playwright/Chromium 用例，8 个通过，用时 16.49s。
- 覆盖：页面加载与 JS error、面板切换、任务表单、worker 记录渲染、详情抽屉、诊断 API、任务预览创建和删除交互。
- 说明：当前会话无 in-app browser 控制接口，按 browser 技能回退到仓库自带的独立 Playwright 测试。
- 下一步：运行 Checkpoint fast check。

## Step 63：Checkpoint fast check（首次）

- 命令：`..\.venv\Scripts\python.exe -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root .`
- 结果：未启动，返回 1。虚拟环境无法直接导入当前源码包：`ModuleNotFoundError: No module named 'visual_agent'`。
- 决定：不安装或修改虚拟环境；仅为下一条命令临时设置 `PYTHONPATH=src`，运行同一 fast check。
- 下一步：带当前源码路径重试 Checkpoint fast check。

## Step 64：Checkpoint fast check（带源码路径）

- 命令：`$env:PYTHONPATH=(Resolve-Path 'src').Path; ..\.venv\Scripts\python.exe -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root .`
- 命令结果：返回 0。运行 `pacer_workbench_static_acceptance`，1 个 workflow、1 个步骤、1 次真实交互全部通过；L3 strict product acceptance 为 1/1。
- Checkpoint verdict：`COVERAGE GAP`。唯一 fallback-only 文件为本次未跟踪的验收记录 `docs/验收进度_2026-07-10.md`，缺少 `docs/` 的精确 affects；产品 workflow 本身通过。
- 决定：不为验收记录文件扩大产品 workflow 覆盖范围，不把 coverage gap 误报为全量 verified。
- 下一步：运行最终 `git diff --check`。

## Step 65：最终空白检查

- 命令：`git diff --check`
- 结果：通过，无空白错误。仅有 5 个已修改 Python 文件的 LF→CRLF 工作区提示。
- 下一步：检查最终 Git 状态与差异摘要。

## Step 66：最终 Git 状态

- 命令：`git status --short`
- 结果：受跟踪改动为 `src/visual_agent/chief_queue.py`、`tests/test_chief_queue.py`、`tests/test_dashboard.py`、`tests/test_mission_plan_import.py`、`tests/test_missions.py`；未跟踪文件为本验收记录。
- 凭据状态：`model_api_keys.txt` 未出现在状态中，MiMo 新 key 保持在 Git 忽略文件内。
- 下一步：审阅受跟踪文件完整差异并形成验收总结。

## Step 67：最终差异审阅

- 命令：`git diff -- src/visual_agent/chief_queue.py tests/test_chief_queue.py tests/test_dashboard.py tests/test_mission_plan_import.py tests/test_missions.py`
- 结果：通过人工审阅。差异仅包含 Windows worker lock 无信号探针修复、锁测试去真实 PID 化，以及 3 个已确认的过期/顺序依赖测试修复；未发现敏感信息或无关产品改动。
- 下一步：运行冲突标记检查。

## Step 68：冲突标记检查

- 命令：`rg -n "^(<<<<<<<|=======|>>>>>>>)" src\\visual_agent\\chief_queue.py tests\\test_chief_queue.py tests\\test_dashboard.py tests\\test_mission_plan_import.py tests\\test_missions.py docs\\验收进度_2026-07-10.md`
- 结果：通过。`rg` 返回 1，表示所有改动文件均未命中冲突标记。
- 下一步：读取最终状态后结束本轮验收。

## Step 69：最终状态确认

- 命令：`git status --short`
- 结果：最终仍为 1 个源码文件、4 个测试文件已修改，加本验收记录未跟踪；MiMo 凭据文件未出现在 Git 状态中。
- 下一步：结束本轮验收。

## 验收总结

- 受影响核心范围：36 个测试文件、602 个不同用例通过。
- 安全排除：`tests/test_chief_queue.py` 的 3 个真实锁场景按用户要求未执行；它们已改为 mock PID 存活结果，但本轮仍保持排除。
- 浏览器验收：8 个真实 Chromium 用例通过，未捕获 JS error。
- Checkpoint：产品 workflow `pacer_workbench_static_acceptance` 的 1 次 L3 真实交互通过；总 verdict 仍为 `COVERAGE GAP`，原因仅是本验收记录缺少 `docs/` 精确 affects。
- 修复项：Windows worker lock 不再用 `os.kill(pid, 0)`；Dashboard worker 测试消除 active workspace 顺序依赖；mission 默认 repair 断言同步为 2；mission import facade 使用可解析的显式验收命令。
- 凭据：MiMo 专属新 key 已从指定文件更新，指纹核对一致，目标文件继续被 Git 忽略；更新后模型凭据测试 14/14 通过。
- 警告：保留 FastAPI/Starlette 依赖弃用 warning、文件锁初始化 warning 和 LF→CRLF 提示；均未造成断言失败。
- 结论：受影响核心功能通过验收；由于 3 个锁场景被主动排除且 Checkpoint 对验收记录报告 coverage gap，不宣称全量 `verified`。

## 本地记忆专项（Memory V2）

## Step 70：GitHub 同类方案调研

- 只读浅克隆并检查 Aider、Serena、OpenHands、Cline 的本地记忆实现与文档。
- 固定调研 commit、参考文件和采用决策到 `docs/本地记忆_GitHub对标与Memory_V2_2026-07-10.md`。
- 采用组合：Aider 的预算与排序、Serena 的渐进披露、OpenHands 的耐久性门槛、Cline 的分层记忆。
- 未复制第三方代码，未引入新依赖。

## Step 71：现有记忆基线

- 活动 mission：31 条；任务目录总数更多，但隐藏/归档任务不进入默认索引。
- 基线查询：`Windows worker lock and mission queue safety`。
- 旧排序选择 8 条伪相关任务，前列为 Workbench/UI preview；handoff 又被 `AGENTS.md` 抢占，只剩 57 字符项目指令，任务经验没有进入 worker prompt。
- 根因：简单词交集、无路径/符号/验证证据、preview 不降权、项目指令会抑制普通 mission fallback。

## Step 72：Memory V2 实现

- 将 mission、plan、worker、verification 和 final report 组合为可寻址 episode。
- 新增字段：`memory_id`、`source_paths`、实际 `changed_files`、计划 `scope_files`、`symbols`、verification command/verdict、worker outcome、match reasons。
- 检索优先级：精确路径、精确符号、目标短语、失败签名、目标词、验收词；preview/inspection 降权，低于阈值不注入。
- 项目指令与 mission episode 同时进入 compact handoff，不再互相遮蔽。
- worker prompt 明确记忆为 advisory/non-exhaustive，不限制 Codex 浏览其他文件。
- 默认 handoff 上限：3 条、1200 字符；完整证据仍可通过 memory ID/source path 查询。
- worker 摘要对 API key、token、secret、password、Bearer 和 `sk-` 形态做脱敏。

## Step 73：增量索引

- 路径：`.agent-workspace/project_memory/index.json`。
- 失效依据：每条 mission 相关源文件的 `mtime_ns + size`，并带独立 index format version。
- 真实工作区首次构建：`hits=0`、`misses=31`。
- 第二次构建：`hits=31`、`misses=0`，不再重复读取 plan、round、worker 和 verification 详情。

## Step 74：专项与回归测试

- 命令：`..\.venv\Scripts\python.exe -m pytest tests\test_project_memory.py tests\test_mission_facade.py tests\test_chief_engineer.py`
- 结果：通过。收集 29 个用例，29 个通过，用时 2.44s。
- 新增覆盖：路径/符号优先、preview 降权、指令与 episode 并存、严格字符预算、证据提取、敏感信息脱敏、索引复用和源变更失效。
- Ruff：`src/visual_agent/project_memory.py`、`src/visual_agent/chief_engineer.py`、`tests/test_project_memory.py` 全部通过。

## Step 75：真实检索对照

- 查询 `Windows worker lock and mission queue safety`：V2 选择 0 条伪相关 mission，只注入 213 字符项目指令；没有历史证据时不编造相关性。
- 查询 `Update pacer-supervised-one-hour-plan.md dogfood record`：正确将实际修改该文件的 `p5-case-a-execute-r7-20260709` 排第一，score 125，理由含 `exact_path`；compact handoff 730 字符，低于 1200 字符预算。
- 历史计划范围与实际 diff 已分离；有 verification diff 时不把宽泛 plan scope 冒充实际 changed files。

## Step 76：Checkpoint 复验

- 命令：`$env:PYTHONPATH=(Resolve-Path 'src').Path; ..\.venv\Scripts\python.exe -m visual_agent.cli codex-check --workspace-root .agent-workspace --repo-root .`
- 产品 workflow：`pacer_workbench_static_acceptance` 通过，1 个 L3 真实交互通过，strict product acceptance 1/1。
- 总 verdict：`COVERAGE GAP`。原因仍是两份 `docs/` 验收/研究记录没有精确 affects；未为追求绿色结果扩大产品 workflow。
- 安全说明：本专项未执行 `tests/test_chief_queue.py` 的 3 个真实 PID 锁场景，未执行任何 `os.kill(pid, 0)` 探针。

## 自主托管与记忆实用性专项（2026-07-11）

## Step 77：自主模式边界

- 新增 `program create --autonomous` 与 `autopilot --autonomous`。
- 自主模式取消 Pacer 自设的 12 任务导入上限、默认串行依赖、45 分钟额度保留和 82% 用量暂停。
- 内部 strong/cheap/research/doc 任务均可进入 delegated worker，由 Pacer 自主排序和排队。
- mission 使用 8 轮、最多 7 个 repair、最长 480 分钟 wall / 240 分钟 worker 的健康边界；这些边界不限制仓库探索、任务拆解或额度分配。
- 生产/外部访问、凭据、restricted path、测试防篡改、验证与手工 merge 门禁保持不变。

## Step 78：真实 dispatch 记忆缺口与修复

- 端到端测试发现：chief plan 会检索并注入 memory，但 `chief_dispatch.build_worker_prompt` 会重新构建实际 worker prompt，原实现没有携带 `project_memory`；切换 agent 时预览 command 还会被删除。
- 修复：实际 dispatch 从保存的 plan 重新生成预算内 memory notes；普通、delegated 和 report prompt 均适用。
- plan `project_memory.usage` 记录 `retrieval_invoked`、`injected_memory_ids`、字符数；dispatch 追加 `dispatch_injected`、`dispatch_memory_ids`，并复制到 `dispatches.jsonl`。
- 这使“建了索引”“计划检索过”“实际 worker 收到”成为三个可区分、可审计的状态。

## Step 79：自主模式与记忆测试

- 纯本地专项：33 个用例通过；覆盖 99% quota 下自主调度、doc/research delegated、14 条任务全部导入、无强制串行、宽预算、delegated 队列参数和实际 worker prompt memory ID。
- 主链回归：`tests/test_chief_dispatch.py tests/test_chief_run.py` 共 127 个用例通过。
- CLI 回归：`tests/test_cli.py` 共 66 个用例通过。
- Ruff：本轮涉及源码和测试全部通过。
- 全部测试使用临时目录/fake runner 或 dry-run preview，没有启动真实 Codex、没有消耗外部模型额度。
- 安全排除继续生效：未运行 `tests/test_chief_queue.py`，未触发真实 PID 锁探测。

## Step 80：Checkpoint 复验

- `pacer_workbench_static_acceptance` 通过，1 个 L3 真实交互通过，strict product acceptance 1/1。
- 总 verdict 仍为 `COVERAGE GAP`，仅因三份 `docs/` 文件没有精确 affects；没有为追求绿色结果扩大产品 workflow。
- 自主托管、实际 dispatch memory 注入及现有产品工作台未出现验证失败。

## Step 81：远程 Codex 自主托管实测（2026-07-11）

- program：`20260711-145031-19babf`；mission：`20260711-145031-695682-270e57-972f63d5`；plan：`20260711-145031-690906-270e57-5aa56bfc`。
- 实际启动 Codex CLI `0.144.0`，模型 `gpt-5.6-sol`，provider `custom`，隔离 worktree，未 merge。
- 结果：worker completed，command gate `rg PACER_REMOTE_CODEX_AUTONOMY_MEMORY_PROBE_20260711 docs/Pacer远程自主实测_2026-07-11.md` 通过，mission/queue 标记 verified/success。
- 成本：729.359 秒，126,992 tokens；产物仅为 35 行文档。主要额外成本来自错误 workspace 搜索、反向查找 plan/memory 和 worker 自行追加 Checkpoint 验证。
- 自主策略写入 mission：8 轮、7 repair、480 分钟 wall、240 分钟 worker、merge=false。

## Step 82：远程实测暴露的问题

- **旧 daemon 热更新漂移**：7 月 10 日启动的常驻 worker 抢先 claim 新任务，使用旧进程内代码；新 queue 的 delegated/usage 字段被旧结构重写，实际 prompt 退回 tracked 模板。
- **实际 prompt 未注入 memory**：原始 Codex user prompt 没有 `Project memory` 或 `memory ID`，反而仍含 `Work efficiently to conserve model budget` 与 `do not scan or grep`。plan 的 `usage.injected_memory_ids` 不能证明实际发送。
- **远程报告自证错误**：模型读取 plan 后把计划注入的 r7/r6 ID 误写成“最终 prompt 实际列出”；本轮不合并该报告，以原始日志为准。
- **额度路由未贯通**：program slot 标记 `cheap_worker`，实际 mission model policy 仍是 strong，最终运行 `gpt-5.6-sol` 并使用 126,992 tokens。
- **验收过弱**：唯一 marker 的 `rg` gate 通过即可 verified，无法识别报告中关于 prompt memory 的事实错误；worker 自行执行的一个 supervised workflow 失败未进入主 command gate。
- **nested dirty overlay 泄密风险**：实测 worktree 可见 `model_api_keys.txt` 文件。已修复 nested copy 的 secret 过滤，并将 autonomous dirty overlay 改为必须显式 `--allow-dirty`。
- **运行产物噪声**：worktree diff 统计包含 7 个 `.agent-workspace` 文件，但 product diff 正确只计目标报告 1 个文件。
- 安全：未向旧 daemon PID 发送信号，未运行真实 PID 锁测试，未自动 merge。

## Step 83：远程实测后安全回归

- autonomous dirty overlay 改为显式 `--allow-dirty`；nested project copy 排除 `.env*`、`model_api_keys.txt`、`api_keys.txt` 和 key/cert 文件。
- `tests/test_chief_dispatch.py tests/test_program_scheduler.py tests/test_programs.py tests/test_hourly_budget.py tests/test_project_memory.py`：124/124 通过。
- Ruff：本轮受影响源码与测试全部通过。
- Checkpoint：`pacer_workbench_static_acceptance` L3 真实交互 1/1 通过；总 verdict 仍仅因 docs affects 为 `COVERAGE GAP`。

## Step 84：当前源码 worker 与显式 memory ID 排序复验（2026-07-12）

- 旧常驻 worker 经命令行、启动时间和孤儿父进程三项身份核对后停止；未使用 `os.kill(pid, 0)`，也未向当前 Codex/pytest 进程发送信号。
- 从当前源码启动 watch worker：PID `16684`，解释器 `D:\助手codex\.venv\Scripts\python.exe`，`PYTHONPATH=D:\助手codex\xiao\src`；lock 启动时间为 `2026-07-11T19:50:02.223648+00:00`。
- Memory V2 新增显式 mission/memory ID 命中优先级；查询包含 ID 时加 120 分并记录 `explicit_memory_id`。新增 `test_memory_v2_prioritizes_an_explicit_mission_id` 已通过。
- 安全边界继续生效：未运行 `tests/test_chief_queue.py` 最后 3 个真实 PID 锁场景。

## Step 85：第二次远程 Codex 记忆注入复验（2026-07-12）

- program：`20260711-195036-bbab39`；mission：`20260711-195036-603160-83d775-7c483ca7`；plan：`20260711-195036-598363-83d775-2a81153f`；queue：`mq-6892b5de63a5`。
- 当前 worker 自动 claim，queue 自然收尾为 `success`，mission 为 `verified`；耗时 55.499 秒，1 次 worker、0 次 repair、1 次 verification，未 merge。
- 实际 dispatch 为 `delegated`；当前 `build_worker_prompt` 明确生成 `Worker mode: delegated`，且不含旧版 `conserve model budget` / `do not scan` 限制。远程 worker 首条消息确认只使用最终 prompt 中可见的 3 条 `Project memory`。
- `dispatches.jsonl` 已生成并记录 `retrieval_invoked=true`、`injected=true`、`dispatch_injected=true`；实际注入 ID 为 `mission:p5-case-c-self-r4-20260709` 和 `mission:p5-case-c-self-r3-20260709`。
- 远程产物逐行写出上述两个 memory ID 和 `AGENTS.md`，并按其内容保持最小改动；marker 命令通过。与首次错误自证不同，本次产物内容与 dispatch 审计记录一致，证明 Pacer 的本地记忆已真实进入并被远程 Codex 使用。
- 模型仍为 `gpt-5.6-sol`，reasoning `medium`；input 84,318、cached input 59,392、output 1,321、reasoning output 126，总计 85,639 tokens。相较首次 126,992 tokens 和 729.359 秒明显改善，但 `cheap_worker` 到实际模型的额度路由仍未贯通，不能把本次视为成本优化完成。
- 新 worktree 中 `.env*`、`model_api_keys.txt`、`api_keys.txt`、`*.key`、`*.pem`、`*.pfx`、`*.p12`、`*.crt` 命中均为 0；secret overlay 修复通过真实远程场景验证。
- 远程分支 `checkpoint/20260711-195036-598363-83d775-2a81153f/track-1-codex` 保持未合并。worktree 状态仅含运行时 `.agent-workspace` 和目标报告，产品变更计数为 1。
- 剩余缺口：当前 command gate 仍只检查唯一 marker，无法独立证明 ID 与事实陈述正确；本轮通过 dispatch 记录人工交叉核验。后续应把 memory ID、dispatch 审计一致性和允许变更文件集合纳入结构化验收，而不是继续依赖 marker-only gate。

## Step 86：Pacer 具体模型路由与 GPT-5.5 真实复验（2026-07-12）

- 根因：`cheap_worker` 原先只影响 queue priority，mission 的 `model_policy` 仍为 `strong`；Codex profile 又采用 `inherit`，所以实际 worker 始终落回全局 `gpt-5.6-sol`。
- 新 program 保存 `autonomy_policy.model_routing`。默认 strong 继承用户配置，cheap/doc 使用官方当前高吞吐档 `gpt-5.6-luna`，delegated fallback 使用平衡档 `gpt-5.6-terra`；delegated 授权模式不再覆盖任务原始 strong/cheap/research tier。
- CLI 新增 `--model`、`--strong-model`、`--cheap-model`、`--research-model`。`--model gpt-5.5` 会覆盖整批任务；分层参数可只切换某一类任务。
- 具体 model slug 已贯通 program -> scheduler -> mission budget -> dispatch command。implementation 和 repair 都会使用明确的 `codex exec --model <slug>`，旧 program 未配置路由时继续 `inherit`。
- 回归：`tests/test_programs.py tests/test_program_scheduler.py tests/test_chief_dispatch.py tests/test_chief_run.py tests/test_cli.py` 共 211/211 通过；Ruff 对 6 个相关源码和 3 个测试文件通过。
- 真实 program：`20260711-201801-023b11`；mission：`20260711-201801-327641-aa412b-376d3c60`；plan：`20260711-201801-323450-aa412b-ab0d948d`；queue：`mq-34dcb9a72aa3`。
- 实际 preview/worker argv 均为 `codex --sandbox workspace-write exec --json --model gpt-5.5 -`，审计字段为 `resolved_model=gpt-5.5`、`model_source=command`。Codex 正常返回 thread/turn 事件，证明当前账户与 CLI 接受 GPT-5.5，没有静默回退到 Sol。
- 结果：queue `success`、mission `verified`，1 次 worker、0 repair、1 verification，耗时 75.557 秒；input 74,745、cached input 59,392、output 718、reasoning output 94，总计 75,463 tokens。
- 远程产物仅 3 行且 marker 验收通过；远程分支 `checkpoint/20260711-201801-323450-aa412b-ab0d948d/track-1-codex` 保持未合并。worktree 中 API key、`.env*`、key/cert 文件命中为 0。
- 结论：模型选择控制已经真实贯通。成本仍未优化完成：三行文档任务约有 15,353 个非缓存 input tokens，下一步应压缩 system/AGENTS/repo context，并对 Sol/Terra/Luna/GPT-5.5 做同任务质量、延迟和 token A/B，而不是只凭模型名称判断节省额度。

## Step 87：五项闭环 Dogfood 暴露并修复的真实问题（2026-07-12）

- Program 中已经具体化且锁定的任务因目标含 `roadmap` 被通用 grounding 再次解释，首轮停止为 `needs_clarification`。修复为 Program 任务显式关闭重复 grounding；调度器 13/13 测试通过。
- Windows npm `.cmd` shim 会把 `model_provider="custom"` 的双引号转义成字面反斜杠，远程 Codex 报 provider 不存在。改用经过 provider ID 校验的 TOML 单引号字面量 `model_provider='custom'`；登录状态解析与 provider/failover 定向测试通过。
- Windows command gate 不接受 shell 单引号表达式；另外 `-k=closed_loop` 选择 0 个用例时严格验收正确拒绝，没有误报 verified。最终 Dogfood 使用 Pacer 虚拟环境的绝对 Python 路径和基线中稳定存在的 `tests/test_mission_progress.py`。
- 阶段 2 的索引曾检索到直接上游 verified mission，但有限 prompt 被旧失败记录占满。修复为下游任务携带 `program_id`、`task_id`、`upstream_mission_ids`，使显式上游 mission ID 获得最高相关性并进入实际 worker prompt。
- 上述失败 Program、mission、verification 均保留为失败证据；没有重写失败状态，没有弱化 gate，没有自动 merge。

## Step 88：最终五项闭环真实验收

- 最终 Program：`20260712-012944-006336`，source plan SHA-256：`5f6d59d117a04215337f960a5c2204b2252d8ebdfb97d43fe3e736e573b25152`，终态 `completed`，两个顺序任务均为 `verified`。
- 阶段 1 mission：`20260712-012944-353245-174268-cafd3b5e`。阶段 2 mission：`20260712-013202-326593-542980-c74d46a4`；阶段 1 verified 后阶段 2才自动创建并由 watch worker claim。
- Codex 路由：两个实际 dispatch 均为 provider `custom`、model `gpt-5.5`、delegated、1 次 worker、0 repair、1 次 command verification；未使用 MiMo。
- 本地记忆：阶段 2 实际 `dispatch_memory_ids` 包含直接上游 `mission:20260712-012944-353245-174268-cafd3b5e`。远程产物准确写出该 ID、`verified` 和 `verification=pass`，与 dispatch 审计一致。
- 托管开发：路线锁 -> 阶段 1 -> 严格验收 -> memory writeback -> 阶段 2 -> 严格验收全程自动推进，Program 最终自行收口为 `All program tasks are verified.`。
- 强验收：最终两个任务都运行 `D:\助手codex\.venv\Scripts\python.exe -m pytest tests/test_mission_progress.py`；此前 provider、环境、0 tests、节点不存在均被拒绝，证明没有用 marker-only 或命令退出码伪造成功。
- Dogfood 路线：source plan SHA 锁定，阶段 2带同一 Program/Task/上游 mission 上下文；没有自然语言临时插入任务或改变依赖顺序。

## Step 89：额度与本地记忆结论

- 最终阶段 2 usage：input 120,188、cache read 102,144、output 1,963、reasoning output 500；实际 memory 注入 751 字符，2 个 mission ID 加 1 条 `AGENTS.md` 指令。
- 可以确认本地记忆减少了重复查找上游 mission 的必要，并让阶段 2准确使用阶段 1结论；不能把 102,144 个 provider cache tokens 归因于本地记忆，也不能仅凭这一次调用宣称节省了多少付费 token。
- 因此本地记忆的“检索、注入、真实使用、闭环优先级”已通过；“货币/付费 token 节省比例”仍需同任务 memory enabled/disabled 的受控 A/B 才能量化。该缺口不再隐藏在 `saved_usd=0.0` 后面。

## Step 90：最终回归、安全与 Checkpoint

- 主工作区专项回归：283/283 通过，用时 71.71s；覆盖 Program、scheduler、Chief run/dispatch、Codex exec、Project memory、CLI、额度、计划导入、mission、验收链和测试防篡改。
- Ruff：`program_scheduler.py`、`chief_dispatch.py` 及对应测试全部通过。
- Checkpoint：`pacer_workbench_static_acceptance` L3 真实交互 1/1 通过；总 verdict 仍为 `COVERAGE GAP`，原因仅为 docs 文件沿用 fallback affects，没有扩大 workflow 来追求绿色。
- 最终两个 worktree 对 `.env*`、`model_api_keys.txt`、`api_keys.txt`、key/cert 文件精确匹配均为 0；未读取任何密钥文件内容。
- 未运行 `tests/test_chief_queue.py` 最后 3 个真实 PID 锁测试，未执行 `os.kill(pid, 0)` 探针；远程分支保持手工 merge。

## Step 91：Pacer 五项闭环网页 Dogfood（2026-07-12）

- Pacer Program：`20260712-014653-2c802b`；mission：`20260712-014653-767338-31bd13-0674870a`；plan：`20260712-014653-761891-31bd13-c65ec1ff`。远程 `custom / gpt-5.5 / delegated` worker 完成页面与本地 API 初版，mission 为 `verified`。
- 页面地址：`/five-pillars`。页面从当前 `.agent-workspace` 的 Program、mission、worker、verification 和 `dispatches.jsonl` 读取真实证据，不写死五项状态。
- 集成审查修正了远程初版的两项事实问题：provider 必须读取 `resolved_provider`，不能用 agent `codex` 代替；直接上游 memory ID 必须读取 `project_memory_usage.dispatch_memory_ids`，不能从不存在的 task 字段推断。无关的 worker 切换改动未集成。
- 实际页面显示：Program `20260712-012944-006336`、provider/model `custom / gpt-5.5`、worker/repair `2 / 0`、直接上游 memory `mission:20260712-012944-353245-174268-cafd3b5e`、两任务 verified 顺序、验收 `pass` 和锁定路线 SHA。
- 交互：全部/已通过/需关注分段筛选、五项行切换证据面板、刷新按钮、主工作台入口均实际可用。
- 浏览器验收：桌面 1440x900 与手机 390x844 均无横向溢出；5 个 pillar、5 个 passed、memory 详情 ID 匹配、console errors 0。截图保存在 `.agent-workspace/probes/five-pillars-desktop.png` 和 `five-pillars-mobile.png`。
- 自动化：Dashboard/API/Playwright 共 66/66 通过；JavaScript 语法检查通过；Checkpoint `pacer_workbench_static_acceptance` L3 数据往返真实交互 1/1 通过。总 coverage gap 仍只来自 docs fallback affects。
- 额度事实：本次 Pacer UI worker input 2,030,466、cached input 1,883,264、output 15,137、reasoning output 2,454。托管节省了用户操作时间，但本轮额度开销明显过高，不能宣称“托管自然省额度”。

## Step 92：外部量化项目极限压力测试与卡死定性（2026-07-13）

- 目标：`D:\海外股票量化`；Pacer launch：`20260713-084439-a9ab243e`，模式为真实 `exec`，原地绑定到 `数字货币量化机器人\crypto_quant_mvp`。
- 运行形成 1 个主代理和 4 个真实子代理；累计 input 6,025,467、cached input 5,455,104、output 132,827、reasoning 79,237，总计 6,158,294 tokens。
- 发生 1 次 compaction；卡死时当前上下文约 30,121 tokens，低于 96,000 自动压缩阈值，因此根因不是当前上下文耗尽。4 个子代理存在真实并行，但累计 token 成本失控。
- 最后 rollout 活动约为本地时间 17:39；之后没有 verification batch、runtime telemetry 或 outcome。机械五项只有 memory active，其余 routing、managed、acceptance、dogfood 均未激活，必须判定托管失败，不能用目标代码测试通过代替 Pacer 闭环。
- 独立只读验收确认目标项目压力测试 8 passed、全套 46 passed、compileall 通过；同时发现不可哈希 mode、异常后的多标的饥饿、零/负止盈阈值误触发等中间态风险。没有替 Pacer 手工修改这些目标代码，避免掩盖托管失败。
- 截至本地时间约 20:37，旧 launcher Python 11668、Codex 21300、Pacer 11632、Node 12152 仍存活。仅用 `Get-Process` 只读查看；未终止、未发信号、未在同一目标项目并发重跑。

## Step 93：记忆、上下文与受管 Python 修复

- 每个 launch 由 root 只加载一次完整 compact memory；首次返回稳定 receipt，后续同 launch/subagent 只返回小于 800 字符的 `memory_reused/not_modified`，不再重复注入完整历史。
- 记忆始终绑定不可变 `launch_goal`；子任务只更新 `query_goal`，并分别保存 goal digest。技能明确禁止默认 `fork_turns=all`，要求最小上下文或无上下文 fork。
- 父目录启动后绑定子项目时会重新选择子项目 `.venv`；Pacer fallback 不再写入 `PACER_PYTHON` 或污染 Codex 全局 `PATH`。项目 venv 保留合法 pytest 插件，只有 Pacer fallback 增加 `-p no:visual_agent`。
- 运行时重绑定专项 67 passed；memory/MCP 123 passed；Python runtime 7 passed。未读取密钥文件，未使用 MiMo。

## Step 94：卡死、并发 launch 与终态状态机修复

- 新增非破坏性 rollout watchdog，区分 `active/idle/unknown/stalled/resumed`；默认停滞阈值 300 秒，只对 `exec/review` 判 stalled，不把交互模式用户空闲误判为卡死，也不 kill、terminate 或 signal Codex。
- watchdog 观测失败进入 `unknown/degraded`，不累计虚假停滞；stop 使用 generation guard，迟到 poll 不能覆盖终态。无论 watchdog/telemetry 是否可用，finish 都强制写入 `monitoring=false`、最终 lifecycle 和 `stopped_at`。
- launch context/manifest 使用按 workspace 的线程锁、portalocker、唯一临时文件、fsync 和 `os.replace`；锁等待上限 1 秒，锁失败只降级遥测并继续原生 Codex。
- 修复 orphan TOCTOU、旧 launch 晚提交覆盖新 pointer、completed pointer 遮挡其他 running launch、终态 context 被旧 liveness sidecar 覆盖等竞争。两个独立 Python 进程的真实 RMW 串行测试通过且无字段丢失。
- Dashboard/support/MCP 状态读取增加 5 秒限频的只读进程 reconciliation；确认 launcher 消失后才锁内持久化 orphan/capsule/event。Windows `OpenProcess` 的 `ERROR_ACCESS_DENIED` 视为进程存在，避免误判。
- MCP 全链保留明确 launch ID；有效 `PACER_LAUNCH_ID` 优先，缺失/非法时才 fallback。A running、B newer/completed 的端到端测试确认不会更新错误 launch。

## Step 95：合并后安全回归与独立复审

- 主线程合并回归：core lifecycle 88 passed、3 个真实进程用例 deselected；MCP 126 passed；support/dashboard 58 passed；memory/CLI 95 passed，合计 367 passed、3 deselected。
- Pacer 批次：`20260713-122731-b6ca8e6e` 5/5 passed；MCP 批次 `20260713-121625-1278e2f7` 3/3 passed。
- 受影响源码与测试 Ruff 全过；compileall 通过；`git diff --check` 通过，仅保留 LF→CRLF 提示。
- 最终只读复审未发现剩余 P0/P1。非阻断残余是 reconciliation 限频为进程内状态，多进程各自最多每 5 秒执行一次只读探测。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行真实 `os.kill`/PID worker-lock 测试，未向旧压力任务进程发送信号。

## Step 96：Pacer 插件更新与重装

- `pacer-native` skill quick validation 通过；插件 validation 通过。
- 按 plugin-creator cachebuster 流程将版本更新为 `0.1.0+codex.20260713123047`，并从 personal marketplace 重新安装；`codex plugin list` 显示 installed、enabled 且缓存路径为新版本。
- 新缓存中的技能已包含单次 memory receipt、最小 fork、真实五项证据、current/accumulated token 分离及新线程续接规则。

## Step 97：重装后的新线程真实闭环

- 新 launch：`20260713-123242-b476df1f`；verification run：`20260713-123456-88ada34f`；真实 provider/model 为 `custom / gpt-5.6-sol`，未使用 MiMo。
- 单批次 3/3 passed：launcher/process-lock 测试 29 passed、两个核心模块 compileall 通过、`git diff --check` 通过。随后 telemetry 和 completed outcome 均成功记录。
- 五项机械终态全部 active：routing observed、memory loaded_with_evidence、managed completed_in_place、acceptance verified、dogfood verified_source_discipline。Memory formal 0、native history 7，未读取 raw history。
- launch 用时 226.016 秒。最终累计 input 127,804、cached input 110,848、output 5,377、reasoning 2,284，总计 133,181；非缓存 input 约 16,956。最终当前上下文 input 21,660、cached 20,224、total 22,810，0 次 compaction。
- 为验证告警，本次临时把 watchdog 阈值降到 15 秒；有效 MCP 等待间隔触发过 stalled 警告，后续 rollout 自动恢复，Codex 从未被终止。默认生产阈值仍为 300 秒。
- 最终 active context、manifest 和 liveness sidecar 一致为 `completed`、`monitoring=false`、watchdog thread stopped。说明新版本已关闭“任务完成但永久显示 running”的路径。
- 成本结论仍需保守：功能闭环和终态已真实通过，但如此短任务仍耗时近 4 分钟且累计 133k tokens；缓存覆盖较高不等于零成本，后续优化应聚焦更短系统上下文、较低 reasoning 档和受控 memory A/B。

## Step 98：极限压力 token 账去重纠错（2026-07-13）

- 原展示 `6,158,294` 并非真实新增 token；四个 fork session 重放了父线程累计账。按 parent thread 和 spawn 时父 usage 去重后，真实新增为 `4,023,162`，原展示虚增 `2,135,132`（34.67%）。
- 真实新增包含 input 3,931,917、cached input 3,550,720、uncached input 381,197、output 91,245；reasoning 59,004 已包含在 output，不能再次相加。cached 占 input 90.30%。
- root 为 2,471,200（61.43%）；四个子代理合计 1,551,962（38.57%）。原始 230 条 token sample 中，126 条为真实 usage 增长、81 条为 fork 前缀重放、23 条累计值未变化。
- 121 个工具调用共返回 447,699 字符，其中 wait 输出占 75.18%；长工具结果进入后续上下文，是缓存前缀持续增长的重要原因。rollout 不提供每段文本的精确 token 拆分，因此字符数不能冒充精确 token 账。
- 物理唯一 memory 调用为 5 次：1 次错误、4 次成功；正常只需 root 一次成功，明确可避免 1 次错误和 3 次子代理 full load。紧邻 memory 的模型轮毛账为 94,054，但不能全部算作 memory 净开销。
- 短闭环 `20260713-123242-b476df1f` 有 7 个真实模型轮：input 127,804、cached 110,848、uncached 16,956、output 5,377、总计 133,181。累计 133k 的主因是 7 轮反复处理约 14k-22k 上下文，不是生成了 133k 新上下文。

## Step 99：Pacer 本地可观测性工作台

- 新增安全的 rollout parser 和 launch-scoped Dashboard API：`/api/observability/launches`、`/api/observability/launches/{id}`、`/api/observability/sessions/{id}/timeline?launch_id=...`。
- 每个 launch 同时展示 raw ledger 与 fork 去重后的 actual、cached/uncached/output/reasoning、逐轮 usage、root/subagent 树、Tool/MCP 时间线、compaction 和五项机械证据；reasoning 明确标注已包含于 output。
- launch manifest 只做快速列表；用户打开详情时才按对应 `.rollout-baseline.json` 解析。baseline、launch 时间窗、repo root 和 parent tree 共同确定归属；同 cwd 多 root 无法唯一归属时返回 `ambiguous` 和零 actual，不猜账。
- API 默认不返回 prompt、response、cwd、文件路径、工具 arguments 或 output。未知正文只保留字符数和 SHA-256；路径必须位于当前 `$CODEX_HOME/sessions`，非法 ID、分页和路径逃逸均被拒绝。
- MCP 时间线从结构化 invocation 提取 `get_pacer_memory`、`run_pacer_verification`、runtime telemetry 和 outcome 名称及 duration；不读取或展示 invocation arguments/result 正文。

## Step 100：真实 rollout 性能与页面验收

- 第一版对真实 launch 首次详情耗时 25,211 ms，原因是归属前完整解析最多 100 个无关 rollout。改为两阶段：先 bounded 读取 session_meta identity + stat，再只完整解析 owned root tree。
- 同一真实 launch 再测为 917-929 ms，约 27.5 倍加速，下降 96.3%；133,181 token、7 轮、54 个事件、16 个 Tool/MCP 事件和五项证据保持一致。
- 性能回归用 100 个无关大 rollout + 1 个 owned rollout 证明只有 owned tree 进入 full parse，不使用脆弱墙钟断言。`limit_sessions` 按完整 root tree 应用，不截断 lineage，超限机械标记 partial。
- 桌面 1440x1000 与手机 390x844 真实页面均显示 actual/raw 133,181、7 轮、16 条工具事件；console errors 0、页面和数字块横向溢出 0。
- 截图：`.agent-workspace/artifacts/pacer-observability-desktop.png`、`.agent-workspace/artifacts/pacer-observability-mobile.png`。本地源码服务：`http://127.0.0.1:18792/`（该次开发会话）。

## Step 101：终态 launch 证据不可变修复

- 工作台子代理在旧 launch 已 completed 后仍调用 verification/outcome，真实把 managed、acceptance、dogfood 从 active 覆盖为 failed；这不是展示错误，而是 MCP 状态机允许 terminal pointer 被重新绑定。
- `_activate_pacer_project` 与 managed Python runtime 现拒绝任何非 running launch；terminal launch 不能再运行 Pacer 验收、绑定项目、写 outcome 或更新 runtime/pillars。
- 新回归同时尝试 late verification 和 late failed outcome，断言 active pointer、per-launch context 字节不变，且不生成 history/commands。旧 launch 三份被本轮子代理覆盖的终态文件已恢复到原通过批次 `20260713-123456-88ada34f`；失败子任务历史和 command 日志保留作审计，没有删除失败证据。
- 兼容旧工作空间：若只有 active pointer 而缺 per-launch context，Dashboard support 仍读取 pointer 的负机械证据，避免回退到历史 completed outcome 造成五项假绿。

## Step 102：本轮回归与明确限制

- Dashboard/rollout/API 专项 82 passed；MCP 全套 127 passed；launcher/telemetry/launch context/process lock/runtime/events/support 107 passed；真实 Chromium 工作台交互 1 passed。相关主链合计 317 passed。
- 新 parser + facade 最终 15 passed；Node syntax、compileall、scoped Ruff 和 `git diff --check` 均通过。
- 尝试运行除 `test_chief_queue.py`、browser/e2e/slow 外的 137 文件全仓回归，364 秒工具上限内未自然结束，因此不能宣称全仓通过；没有把 timeout 改写成 pass。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行其当前 pytest PID worker-lock 场景，未向 Codex/pytest/旧压力任务发送信号。
- 当前为可靠的本地 metadata 工作台，不是 Sub2API 网关的 1:1 复制。订阅模式无法补出上游 HTTP request ID、状态码、重试、流式 chunk、真实货币账单和精确首 token 网络延迟；若流量经过 Sub2API，可后续按 request ID 联表。

## Step 103：同任务受控 Direct/Pacer A/B 与成本根因（2026-07-14）

- 冻结基线 commit `c1a3484328e4bfad8710e1d4a455d7d037a8cea5`，任务 SHA-256 `f2fd644e60d8535229d9f877d1fac454d596bb919490fc5ccb13462fe15d6fdb`；所有 lane 使用 Codex CLI 0.144.0、`custom / gpt-5.6-sol / ultra`、workspace-write、approval never、network false、96k auto compact，均无子代理和 compaction。
- Direct：227,804 total tokens、221.390s、12 模型轮。旧 Pacer：524,949 tokens、350.666s、21 模型轮，比 Direct 多 130.44% tokens、慢 58.39%。
- 旧 Pacer 的 6 个失败 schema/outcome 调用关联 172,531 tokens 和 51.707s，而全部 MCP 本体只执行 1.396s。主成本是失败后重放上下文，不是本地 MCP 的 CPU 时间。
- 即使乐观删除全部失败轮，旧 Pacer 仍预计比 Direct 多 54.70% total tokens，因此只修 schema 不足，必须合并 verification、telemetry、outcome 并减少模型轮。

## Step 104：Memory 预载、原子完成与直接控制契约

- launcher 在真实任务已知时预载 compact Memory，注入 receipt/payload/原始 goal；模型不再重复调用 Memory。fresh repo 的最终样本为 109ms、1,132 字符、formal=0、native=0、`effective_hit=false`，能力 active 但不伪报历史命中。
- 新增 `complete_pacer_task`，一次调用执行 allowlisted verification、同 launch runtime telemetry 和 outcome；成功响应不带大 stdout/stderr，失败只带 bounded decisive tail。
- Pacer CLI 不再自动前缀 `$pacer:pacer-native`，改为约 1,157 字节的直接控制契约；plain Codex 保持隔离，插件 skill 仍可手工调用且 `allow_implicit_invocation=false`。
- 控制契约禁止 SKILL/plugin 读取、ALL_TOOLS/MCP schema/server discovery 和 `codex mcp list`；要求批量独立读取、不重复 unchanged file/status/diff、final acceptance 只通过原子完成执行。

## Step 105：五项“假绿”与归因串账加固

- trusted verification 增加 MCP 进程内 HMAC-backed registry，绑定 workspace、launch ID、run ID 和 summary digest；引用 run 时必须提交同进程返回的 receipt。磁盘 `summary.json` 只作结构审计；手写、复制、篡改或只复用 run ID 不能授予 Acceptance。
- compile-only 不再让五项全绿；至少需要真实通过的 test/build/analyze。trusted gate 拒绝 `ruff --fix/--unsafe-fixes`、`git diff --output` 和 pytest cache/snapshot 更新参数。
- `complete_pacer_task` 在 verification 前只解析一次 launch 并 pin；verification 前、过程中、telemetry 阶段 pointer 漂移均不能改绑另一 launch。
- `-C/--cd/--cd=/-Cfoo` 的 Memory、managed Python、baseline、watchdog、workspace 和 source responsibility 绑定 Codex 的 effective repo root；原 argv 和 subprocess cwd 不变，不存在路径不被 Pacer擅自创建。
- 每个新 launch 注入唯一 ownership marker。Routing 只有在 `captured + high + ownership matched + 非 MiMo` 时 active；同 cwd plain Codex 不会被认领，旧 cwd/time 证据降为 legacy low 且不能点绿。
- launch 记录写入失败时仍给子 Codex 唯一 `PACER_LAUNCH_ID`，使 MCP fail closed，不会 fallback 污染另一个 running launch。
- Pacer event 增加跨线程/跨进程递增 sequence；同 timestamp 的 started/finished 顺序稳定，旧事件按 timestamp 兼容读取。
- 命令日志脱敏改为显式 fail closed：替换失败时覆盖/移除原输出，验收步骤失败，不读取 raw tail，不返回日志路径；新增两项回归覆盖文件替换失败和调用层 tail 隔离。

## Step 106：最终直接控制样本

- 保留第一轮负样本：238,945 tokens、208.360s、12 模型轮、10 command items、1 MCP。它虽然没有技能读取/schema 重试，却重复单独运行 final acceptance，证明取消 skill 本身不保证省 token。
- 加入去重复控制后的最终样本：launch `20260713-204635-cd4871dc`，run `20260713-204919-7b39a1e5`；171,086 tokens、177.010s、9 模型轮、6 command items、1 次 `complete_pacer_task`。
- 最终样本相对 Direct：total tokens `-56,718`（`-24.90%`），wall `-44.380s`（`-20.05%`）；相对原子完成初版：tokens `-7.77%`、wall `-8.41%`。
- 但最终 uncached input 为 22,074，仍比 Direct 的 19,807 高 11.45%；cached input 144,128 是 input 子集，不能把 total token 下降直接换算成付费金额。
- 最终当前 context input 23,236，约为 96k 阈值的 24.20%；累计 input 166,202 来自 9 轮重放，不代表上下文膨胀到 166k。
- 公开测试 7/7、独立隐藏测试 5/5，含自定义 int 子类、bool/非整数、未知资源、累计超限、顺序和输入不变性。五项全部机械 active；Memory 明确为空命中。

## Step 107：最终回归与可安装产物

- 最终安全矩阵 469/469 passed，用时 152.59s；覆盖 launcher、rollout ownership、launch context、events、CLI、MCP、managed Python、support、Dashboard、observability、Memory、context 和日志脱敏 fail-closed；JUnit 与 stdout/stderr 已持久化到 A/B 报告目录。
- Ruff、compileall、`git diff --check` 均通过；仅有 CRLF 提示。
- 最终 wheel `pacer-final-wheel-20260714-v3/visual_agent-0.1.1-py3-none-any.whl` 构建成功，SHA-256 `2f66623066b90bc8b4bfe1eab86a04c64f3652ef36c85b12065ff1d2eb9fe491`；六个新增核心模块及 missing-launch/receipt/log-redaction 加固均进入 wheel。
- Python 3.11 隔离 venv 从 wheel 安装成功，核心模块确认从 wheel site-packages 导入，`pacer --version` 返回 `codex-cli 0.144.0`。
- Python 3.11 宿主的 setuptools 65.5 在 `--no-build-isolation` 下不支持当前 license 元数据；项目已声明 `setuptools>=77`，使用声明工具链构建成功，未把旧构建器限制误报为源码失败。
- 安全排除保持：未运行 `tests/test_chief_queue.py`、当前 pytest PID worker-lock 用例、`os.kill`/taskkill/信号或进程终止探测。

## Step 108：当前产品判断

- 五项闭环已达到“可真实使用的第一版”：每项都有机械状态和失败条件，Acceptance/ Routing 的主要假绿路径已关闭，Pacer CLI 仍是原生 Codex 交互与能力主体。
- 托管与 Dogfood 的实际价值首先是减少用户盯守、保持原地负责和自动收口，不应宣称天然减少 provider tokens。token 节省来自减少失败轮、重复检查、历史扫描和上下文重放。
- 当前最终单样本优于 Direct，但模型非确定性明显；必须保留 238,945-token 负样本。下一阶段应对 Direct/最终 Pacer 各跑多次随机重复，并做 memory on/off 同任务对照后再给长期比例。
- 完整 A/B 表、token 分解、五项证据和限制见 `.runs/pacer-ab-v2-20260714/report.md`。

## Step 109：Pacer 任务审查、目标契约与证据防篡改（2026-07-14）

- 新增确定性的任务审查层：最终报告固定为普通用户可读的中文“目标、实际完成、未完成、证据、阻断、风险、可信度”，Codex 自报文字不能单独把任务点绿。
- `begin_pacer_task` 必须在读取或修改仓库前锁定原目标，并把并列目标拆成稳定 requirement ID。`complete_pacer_task` 的每条 claim 必须覆盖已锁定 ID；未知 ID、漏项、替换目标、空泛总结和明确未完成项均阻断。
- 文件角色由真实路径判定，不采信 claim 自报类型。产品修复必须有 implementation 文件，新测试必须有 test 文件，文档任务必须有 documentation 文件；`tests/test_*.py` 不能冒充产品实现，未识别的开发措辞默认要求产品源码变化。
- 每条 claim 必须绑定至少一个通过的 test/build/analyze。聚焦验收命令还必须与该 claim 的需求或文件有可见关联；无关的绿色测试不能替交付背书，全量验收命令仍可作为项目级证据。
- source baseline 和 task contract 均增加 MCP 进程内 HMAC 收据，绑定 workspace、launch、repo、目标和内容摘要。篡改 baseline、同时替换 `launch_goal`/contract、删除 receipt 或模拟 MCP 重启，都会在 verification 启动前 fail closed；verification 后和 outcome 前再次复核。
- 公开 `record_pacer_outcome` 仍只允许 `failed/blocked`；`completed` 只能由原子完成路径在 trusted verification、有效 task review、同 launch/run 绑定全部成立后内部写入。
- 对抗覆盖包括：测试文件伪装实现、`支持暗色模式` 伪装只读、并列需求只做一项、契约篡改、目标与契约同时替换、baseline 篡改、receipt 缺失、registry 丢失和无关聚焦绿测。
- 最终回归：`tests/test_mcp_server.py` 176/176 passed；task review、response、launcher、launch context、support 合计 156/156 passed；`compileall` 和 `git diff --check` 通过，后者仅有 LF/CRLF 提示。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行当前 pytest PID worker-lock 场景，未向 Codex/pytest 进程发信号。
- 明确边界：MCP 进程在任务中途重启后不会从可写磁盘重新授信，当前需要新 launch 重做握手；文件名/命令目标关联能阻断明显无关绿测，但不能数学证明测试内部断言完整覆盖业务语义，仍需真实项目 Dogfood 继续发现弱验收。

## Step 110：启动前可信任务登记（2026-07-14）

- Pacer launcher 在启动 Codex 子进程前解析真实任务，生成不可变 task contract，并捕获源码 baseline；模型不再决定“原始源码”从哪个时间点开始。
- launcher 只通过子进程环境传递 contract/baseline SHA-256。MCP 可读取但模型的子 shell 不能修改既有 MCP 进程环境；磁盘文件、active launch 或目标被替换后均与启动摘要不符并 fail closed。
- `begin_pacer_task` 从“首次捕获基线”降为可信采用握手。普通手工初始化且没有启动摘要的旧路径仍保持原有进程内 HMAC 行为，不会无条件重信任磁盘。
- MCP 重启现在可以恢复同一 launch，但前提是继承原 launcher 摘要且 contract、goal、workspace、repo、baseline 全部匹配；没有摘要、摘要缺失或内容被篡改仍拒绝。
- 新增回归覆盖：Codex 启动前已存在 contract/baseline、跨进程采用成功、baseline 篡改拒绝。启动器、launch context、MCP 完整集合 `297 passed`，相关 Ruff 通过。
- 今日 checkpoint：`34bc5e1`；收尾门禁 `384 passed`，最新 wheel 为 `.runs/pacer-final-wheel-20260714-v4/visual_agent-0.1.1-py3-none-any.whl`，SHA-256 `9566f7f2b3312029995cbc60dee2b897912005ec297ae614097b2f7c5f13f789`。

## Step 111：可安装 Pacer 跨进程握手与真实闭环加固（2026-07-14）

- Python 3.11 wheel 安装的慢下载已通过镜像解决；v5 的基础、MCP、Cloud extras 均完成安装，`pip check` 无冲突，核心模块确认从 wheel `site-packages` 导入。
- 第一轮真实负样本证明 fail closed：MCP 未启动时 Codex 未读取、未修改仓库；启动前 task contract、源码 baseline 和 `task_pre_registered=ready` 已落盘，没有伪造 begin/complete。
- 真实故障根因是 Codex MCP 子进程默认不继承自定义 `PACER_*` 环境，而且单独覆盖 `mcp_servers.pacer.command` 会清空原 `args`。launcher 现在用当前 Pacer Python 注入完整 command/args/env_vars，转发 launch ID 和三个预启动信任字段，并清除父进程可能残留的旧摘要；begin 控制模板明确使用 `goal` 字段。
- 第二个 Dogfood 故障是“修复实现，使现有测试通过”被误判成必须修改测试文件。任务分类现在区分“测试是交付物”和“测试是验收目标”；结果关联也不再按长结果文本的 50% 非对称阈值误报，同时保留无关 `fix payment` 对 `fix login` 的低关联警告。
- v7 真实 wheel 闭环：launch `20260714-155206-0dbba200`，verification run `20260714-155546-3227fe8a`。begin 采用启动前合同成功，工作区只修改 `calculator.py`，`python -m unittest discover -s tests -v` 实际执行 1/1 passed，history outcome 为 `completed`，五项支柱全部 active。
- 真实事件顺序为 `launch_started -> task_pre_registered -> memory_loaded -> verification_batch_finished(passed) -> outcome_recorded(completed) -> launch_finished`；task contract receipt、source baseline receipt、verification summary 和同 launch/run 绑定均存在。
- v7 的完整链路只剩两条 `claim_result_low_overlap` 假警告。v8 Python 3.11 隔离环境从 wheel 安装后，用同一真实 launch 的原 baseline、合同、claims 和 verification summary 重放最终审查，得到 `valid=true`、`verdict=approved`、`trust=yes`、`warnings=[]`、`can_trust=yes`。该项是同证据的安装态审查重放，不冒充第二次模型执行。
- 最终安全回归 `376 passed`；Ruff、compileall、`git diff --check` 和 Checkpoint L3 真实交互通过。未运行 `tests/test_chief_queue.py`、当前 pytest PID worker-lock、`os.kill` 或进程终止探测，也未停止现有 CMD、Codex 或项目服务。
- 最终候选 wheel：`.runs/pacer-final-wheel-20260714-v8/visual_agent-0.1.1-py3-none-any.whl`，SHA-256 `1325c991f3d491d8b6d1576a5f9b8328cd56f838b5f83054efa9e903ff7bab37`。

## Step 112：0.1.2 默认安装契约与最终稳定性矩阵（2026-07-15）

- 版本提升为 `0.1.2`；`mcp>=1.0.0` 进入基础依赖，默认安装即可启动 MCP。launcher 缺少 MCP 时以退出码 78 fail closed；发布元数据、CLI 和启动检查回归已补齐。
- Python 3.11 裸安装未使用 `[mcp]` extra，自动安装 `mcp 1.28.1`；`pip check`、`visual-agent --version`、`pacer --version` 和 site-packages 导入均通过。
- 第一轮矩阵发现文档复合要求被误判为产品实现；第二轮发现 `不要修改 validator.py` 被误判为正向实现要求。两次都由 Pacer fail closed，失败 wheel/矩阵保留，不冒充正式结果。
- v3 引入纯文档合同的定向 compile-only 例外，普通实现任务仍拒绝；否定修改短语不再触发正向 mutation。v4 保留这些规则并补齐读侧一致性，完整中文任务和 MCP 闭环回归均通过。
- v3 继续发现 support/Dashboard 读侧会把已通过的文档 compile-only 批次降级；v4 已统一读写策略，3 个最终文档样本读侧均保持 `verified_batch`。
- 最终安装态矩阵根目录 `.runs/pacer-stability-0.1.2-20260715-v4`：实现修复、新增测试、只读审查、文档修改各 3 次，合计 12/12 completed、12/12 `can_trust=yes`、0 warnings、0 外部 5xx。
- 总墙钟 1,397.203 秒，平均 116.434 秒；总 tokens 1,590,805，平均 132,567。新增测试类 3/3 有证据状态重提，共 6 次拒绝，均未执行验收或误报成功。
- 分批回归按设计环境 594 项通过；读侧修复后关键子集 231 项再通过。单进程全仓测试在 604 秒工具上限超时，未记为通过。
- 最终 wheel `.runs/pacer-release-0.1.2-20260715-v4/visual_agent-0.1.2-py3-none-any.whl`，SHA-256 `429AE92D2F7C6C7917C5AF0EF3A9160CC01303E67D0AAA94ABB0FFB439B544E6`。完整明细见 `docs/release_0.1.2.md`。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行 PID/worker-lock、`os.kill` 或进程终止探测，未关闭现有 CMD、浏览器、Codex 或项目服务。

## Step 113：GitHub 对标后的服务端文件事实与安装态泛化（2026-07-15）

- 对 `src/visual_agent` 164 个 Python 模块完成职责拆分，并固定读取 Aider、OpenHands、SWE-agent、Goose、Cline、MCP Python SDK、Playwright、Robot Framework、Celery、Prefect、Git、OpenTelemetry Python 共 12 个仓库的具体实现文件与 commit。完整决策见 `docs/Pacer_GitHub对标与重构审计_2026-07-15.md`。
- 完成证据的文件事实改由服务端从可信 launch baseline、`git status --porcelain=v2 -z`、`git diff --name-status -z` 或非 Git 有界指纹扫描推导；模型只提交 requirement IDs、结果和 verification step。staged/unstaged/untracked/committed/rename、启动前脏文件、protected path、只读越界、扫描不完整和 unmerged 均有 fail-closed 覆盖。
- 最终控制提示删除旧 `kind/requirement/files/result_kind` 模板，明确 protected/read-only 声明复用实质验收步骤，禁止为文件保护单独增加 Git status/diff 步骤；拒绝响应返回结构化 correction、错误 code 和服务端变化集。
- 安装态新增测试三轮从 2 次完成调用收敛到 1 次；最终轮 83,391 tokens / 98.703 秒，相对旧同类均值 193,666 tokens / 153.547 秒下降约 57% / 36%，证据拒绝从每条 2 次降为 0。
- 最终协议泛化覆盖新增测试、实现修改、只读分析、文档修改四类：4/4 approved、4/4 `trust=yes`、0 warnings、0 证据重提，平均 84,054 total tokens / 99.571 秒。服务端分别识别 `created`、`modified`、零变化和 documentation-only 变化。
- 文档真实样本发现逗号拆分后“运行方式小节”丢失 README 上下文。v4 增加窄范围文档内容继承，保留 `修复 app.py` 为 implementation；原任务完整走服务端派生证据、protected `app.py` 和 compileall 回归后一次通过。两次前置负样本均 fail closed，未计入成功率。
- 当前安装态候选 wheel `.runs/pacer-github-refactor-0.1.2-20260715-v4/visual_agent-0.1.2-py3-none-any.whl`，SHA-256 `0A3AAF0DBB9CE4C726197F22D8B997F4152D511429164C75E87FCEC2588D8F11`；Python 3.11 `pip check` 通过。
- 最终定向回归 391 passed：task review + MCP 223、launcher/support/MCP response 95、Dashboard 73。Ruff、compileall、`git diff --check` 通过；Checkpoint 精确覆盖，`pacer_workbench_static_acceptance` 2 个步骤、2 次真实交互通过，L3 strict product acceptance 1/1，verdict `PASS`。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行 PID/worker-lock、`os.kill`、taskkill 或进程终止探测，未关闭现有 CMD、浏览器、Codex 或项目服务。

## Step 114：Pacer 六阶段全面提升收尾（2026-07-15）

- 五项机械语义、Acceptance Contract、Memory/Routing 证据链、Managed 状态机与硬预算、外部签名 Dogfood、3 仓库 x 5 场景 Release Matrix 已落盘；Dashboard 统一展示四态，不再把普通完成自动投影为五项全绿。
- Chief 的统一 ChangeSet 边界已修复：扫描不完整不能作为产品变化证据；受限 tracked runtime path 由额外 Git `diff --name-only -z --relative` 审计捕获，`.agent-workspace` 篡改继续被 scope gate 拒绝。Chief/Mission 主回归 `181 passed`，MCP + 五项完整回归 `190 passed`。
- 新增模块审计与入口专项 `25 passed`；直接依赖补齐 `pydantic>=2.11,<3`。真实 wheel 验证发现并修复 `pacer.exe` 将 release 命令误转发给 Codex 的入口缺陷，全部 `QUALITY_COMMANDS` 现由 Pacer CLI 处理，`resume/exec` 等 Codex 转发保持不变。
- 最终 wheel `.runs/pacer-six-stage-wheel-20260715-final3/visual_agent-0.1.2-py3-none-any.whl`，SHA-256 `8441AD7061992253F0E44E58D07366550F08DA7935007DEDC094A19252EF0746`。全新 Python 3.13 venv 安装成功，`pip check` 无冲突，typed MCP、Release Evidence、ChangeSet、OTel 均从 site-packages 导入，包内 workflow 已包含 `.pacer/` 精确覆盖。
- 六阶段收尾时 `.pacer/release.json` 摘要为 `1e45d734a04929ef0845f5ad857641f985f899da5b4fc1473ec33a37010a380e`；Dogfood 专项改变证明身份与 100 分 case 后，新摘要为 `aaa50981eb0ed72d2b1402303b6010828f022aef3da198ee7563dbcb5c84802a`。仓库没有外部签名 `.pacer/dogfood-evidence.json`，`pacer-dogfood-check` 按预期非零退出，未伪报三次 Dogfood 或 release-ready。
- 变更文件 Ruff、compileall、`git diff --check` 通过；Checkpoint 精确覆盖 `.pacer/`，L3 真实交互 1/1，strict verdict `PASS`。完整 Ruff 仍报告 8 个本轮未触及的历史问题；旧 CLI fixture/dry-run 全套仍有 5 个既有断言失败，均单独记录且未算作通过。
- 安全排除保持：未运行 `tests/test_chief_queue.py`，未执行 PID/worker-lock、`os.kill`、taskkill 或进程终止探测，未关闭现有 CMD、浏览器、Codex、Dashboard 或项目服务。

## Step 115：Dogfood 95 分专项重构（2026-07-15）

- 对照 GitHub Artifact Attestations、SLSA Generator、Ruff、Pants、Sigstore/Cosign 和 uv 的固定 commit 实现，新增 `.pacer/dogfood.json` 三级标准：Local 85、CI 95、Release 100；policy digest 为 `f68d8ff4950192328f3e6480f3d71f4b3f71b063a0c61050f21059953f0f6f9d`。
- 新增 `github_attestation.py`，只调用 GitHub CLI 维护的 `gh attestation verify`，不自研 OIDC/DSSE/Sigstore；candidate wheel 和 canonical evidence 必须分别通过 subject、repository、signer workflow 校验。
- 新增可复用 `.github/workflows/pacer-dogfood.yml`：最小 job 权限、完整 SHA pin、独立 artifact 下载、双 subject attestation、fresh install、`pip check`、95 分门禁和验证结果 attestation；禁止 `pull_request_target`。
- 修正连续运行语义：三次验证必须针对同一个不可变 candidate wheel，evidence 和 GitHub run identity 各自唯一；candidate 漂移、重复 evidence 或重复 run identity 均重置 streak。旧“重复同一 wheel 即失败”的规则已删除。
- HMAC-only 完整本地证据最多 85，不能再把本地完整性误报为 CI 成熟度；GitHub provenance 达到 95，独立 run identity 达到 100。Dogfood 支柱也要求 `quality_target_met=true` 才能 passed。
- 当前仓库仍无真实 canonical evidence 和远端 OIDC bundle，因此没有宣称已达到 95；需要一次真实 GitHub A-to-B run 才能机械达到 95，同一候选三次独立 run 才能达到 100。完整设计与 GitHub 链接见 `docs/Pacer_Dogfood_专项重构_2026-07-15.md`。
- 专项候选 wheel `.runs/pacer-dogfood-95-20260715-v2/visual_agent-0.1.2-py3-none-any.whl`，SHA-256 `E7D27FD63E22BF851D627DDDF858B0B394993CA14D40E0231EEFAD26821B923E`；全新 Python 3.13 venv 的 `pip check`、policy CLI、manifest lock、site-packages 导入与包内 workflow 通过，缺真实 evidence 时继续 fail closed。
