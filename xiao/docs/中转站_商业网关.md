# Pacer 商业中转站

Pacer Gateway 是现有 `cloud_api` 内的一套单机商业试点网关。它面向拥有合规 API 使用与转售权的 OpenAI-compatible 上游，不转卖个人订阅、不导入浏览器 Cookie，也不复用 Codex 登录凭证。

## 五分钟启动

安装 Cloud 依赖并生成管理员凭证：

```powershell
python -m pip install -e ".[cloud]"
python -m cloud_api.setup_gateway --env-file cloud_api/.env.gateway
```

第二条命令只把管理员 token 的哈希与盐写入环境文件，明文 token 仅在当前终端显示一次。随后把真实上游 key 填入 `cloud_api/.env.gateway`，变量名可以自定，例如 `PACER_UPSTREAM_OPENAI_KEY`。

启动：

```powershell
docker compose -f docker-compose.gateway.yml up -d --build
```

打开 `http://127.0.0.1:8000/gateway`，依次完成：

1. 创建套餐和客户。
2. 配置公开模型、售卖价格和保守的上游成本。
3. 新增 OpenAI-compatible 上游，`secret_env` 填环境变量名，不填密钥值。
4. 测试线路并签发客户 key。
5. 客户把 Base URL 设为 `http://127.0.0.1:8000/v1`，API key 使用 `pacer_sk_...`。

客户也可以直接打开 `/billing` 使用邮箱账户登录。首次注册需要邮箱验证码，服务端会在
SQLite 中保存账户、密码哈希、会话和账户到租户的绑定；登录 Cookie 与 `pacer_sk_*`
API Key 是两套凭证。开发或演示环境可设置 `PACER_ACCOUNT_DEV_CODES=1` 在接口响应中返回
验证码，生产环境应接入邮件发送适配器，不应开启该开关。账户注册会原子创建 Starter
租户和默认 API Key，API Key 明文只在注册响应中返回一次。
公网 HTTPS 部署时设置 `PACER_ACCOUNT_COOKIE_SECURE=1`，让会话 Cookie 只通过 HTTPS 发送。

启用微信 Native 充值时，再配置 `PACER_WECHAT_*` 商户参数和
`PACER_WECHAT_CREDIT_PACKAGES_JSON`，将商户平台支付通知地址设为公开 HTTPS 的
`/api/gateway/billing/wechat/notify`，客户从 `/billing` 进入额度中心。套餐金额与
发放额度只从服务端配置读取，前端不能自定义。

商户私钥和可选的平台证书放在被 Git 与 Docker 构建上下文排除的
`cloud_api/secrets/`，Compose 会把它只读挂载为 `/run/secrets`。不要把密钥放进
镜像、README、截图或环境文件；`.env.gateway` 只保存密钥文件路径和 API v3 Key。

## 已落地闭环

- 多租户、套餐、客户 API key；客户 key 只保存 salted SHA-256，明文只返回一次。
- `/v1/models`、`/v1/chat/completions`、`/v1/responses`、`/v1/embeddings`。
- 多条同构 OpenAI-compatible 上游的优先级、权重、并发上限、熔断和首块前切换。
- 每分钟限流和并发限制同时按客户与 key 执行，不能靠多发 key 绕过套餐。
- 请求前余额预占，结束后按冻结价格快照结算；最终扣费不超过预留，不产生负余额。
- 流式终态校验、拆分 usage 合并、客户端断开后继续读取上游、长流心跳续租。
- 请求幂等键、请求指纹和逐次上游 attempt 记录；同键异载荷拒绝。
- 崩溃恢复区分“尚未发给上游”和“结果不确定”：前者自动释放，后者进入人工 capture/release 对账。
- 付费套餐只有在续费期间内才能鉴权和预留；外部支付号在所有客户、充值和续费流程中全局唯一。
- attempt 记录上游 request ID、token 和上游成本；模糊的 2xx 读取失败不会切换线路，而是保留预占等待人工对账。
- 请求日志不保存 prompt 或响应正文；上游明文 key 只从运行环境读取。
- 管理工作台包含客户、套餐续费、余额、key 生命周期、上游健康、价格、请求和资金账本。
- 微信 Native 下单、二维码、签名回调、平台证书自动刷新、主动查单、关单和幂等入账；无余额或订阅过期的客户仍可用有效 key 充值。
- 管理员可对回调延迟的 Native 订单执行主动查单恢复；恢复仍复用商户、交易类型、金额和全局交易号校验，不接受运营手工填写金额入账。
- 邮箱账户注册、验证码一次性消费、登录/退出、密码重置和 HttpOnly 会话；会话可以直接访问 `/billing`，不把登录 token 当作 API Key。
- 登录账户可查看所属租户的 Key 元数据，自助创建和撤销 API Key；新 Key 明文只在创建响应中返回一次。

客户端应为每个可重试的逻辑请求发送稳定的 `Idempotency-Key` 或 `X-Request-Id`。没有这两个头时，网关会为了 OpenAI 客户端兼容性生成请求 ID，但无法识别客户端侧的重复提交。

## 金额口径

后端所有金额使用整数 `micro-USD`：`1 USD = 1,000,000 micro-USD`。价格字段表示每一百万 tokens 的 micro-USD 值。工作台自动换算为 USD。

请求账本的 `reserve` 是预占，`settlement` 是预占与实际扣费的差额，`release` 是失败退款。`actual_microusd` 是已消耗余额，不是支付机构的现金到账额；工作台因此把“计费额”和来自充值、套餐确认的“已确认收款”分开显示。毛利是用量计费额减上游成本，不是现金流利润。

套餐续费接口要求管理员提供已经核实的外部交易号，并按套餐月费校验到账金额，再幂等发放包含额度。它适合人工转账、合同客户或小范围收费试点。

人工 `capture` 可填写最终客户扣费、上游成本和 token 数；少于预占的部分会自动退回客户余额。支付流水使用全局外部交易号，实际接入时建议带渠道前缀，例如 `bank:20260714:000123`。

## 微信 Native 安全边界

网关对微信请求使用商户 RSA 私钥签名；响应和回调必须通过微信平台证书验签，回调资源再用 API v3 Key 解密。只有 AppID、商户号、`NATIVE` 交易类型、`SUCCESS` 状态、`CNY` 币种和订单金额全部精确匹配时才会入账。微信交易号在全局唯一表中占位，订单置为已支付、余额增加和账本记录在同一个 SQLite 事务内完成，因此重复回调和查单恢复不会重复发额度。

商户私钥与 API v3 Key 只能通过服务端环境或只读 secret mount 提供，不会下发到 `/billing`。二维码只编码微信返回的 `code_url`。生产通知地址必须是公开 HTTPS；商户平台中遗留的测试域名或根路径不能用于回调。

## 当前商业边界

当前自动支付只覆盖微信 Native 的额度购买，没有支付宝、Stripe、自动退款和支付机构日终账单导入。管理员查单恢复只解决回调延迟，不等于退款或财务对账；退款需要运营人工核对额度消耗后处理，在退款冲销账本和支付机构日账单核对完成前，不能宣称完整财务闭环。

第一版只接受同步、文本输入、token 计费的请求以及自定义 function tools。`background=true`、图像/音频/文件输入，以及 web search、file search、code interpreter、hosted shell、image generation、MCP 等上游另行计费的托管工具会在请求发往上游前拒绝。要开放这些能力，必须先增加任务终态轮询和对应的按次、存储或容器计价器。

多条上游必须使用相同的 OpenAI-compatible 协议、公共模型映射和成本合同，并填写相同的 `routing_contract`。同一公开模型存在多个候选但合同标识为空或不一致时，网关会停止该模型的路由。若实际供应商的模型别名或成本不同，应拆成不同公开模型；当前版本不做异构协议转换。

当前存储是 SQLite，适合单机和小流量试点。多节点部署前应把余额事务、租约、RPM 和并发状态迁移到 PostgreSQL/Redis，不能让多个容器各用一份本地数据库。

所有远程部署都应在反向代理后启用 HTTPS，限制 `/api/gateway/admin/*` 的网络来源，并备份 `/data/gateway.db`、WAL 和审计日志。不要把 `.env.gateway`、上游 key、客户 key 或数据库提交到 Git。

## 参考实现

设计对照了 Sub2API 固定提交 `7d239d62e8f1c6aea79164f88903f4158cbf2f98` 的公开机制，但没有复制其 LGPL 源码：

- [hold/capture/release 与去重事务](https://github.com/Wei-Shaw/sub2api/blob/7d239d62e8f1c6aea79164f88903f4158cbf2f98/backend/internal/repository/usage_billing_repo.go#L22-L109)
- [过期任务恢复与心跳复核](https://github.com/Wei-Shaw/sub2api/blob/7d239d62e8f1c6aea79164f88903f4158cbf2f98/backend/internal/service/batch_image_billing_recovery.go#L26-L90)
- [流式终态、断线 drain 和 usage 合并](https://github.com/Wei-Shaw/sub2api/blob/7d239d62e8f1c6aea79164f88903f4158cbf2f98/backend/internal/service/gateway_upstream_response.go#L788-L1048)
- [支付回调与对账边界](https://github.com/Wei-Shaw/sub2api/blob/7d239d62e8f1c6aea79164f88903f4158cbf2f98/docs/PAYMENT.md#L184-L261)
